# Product Build Run Slice Handoff Ledger

## Purpose

This ledger is the canonical slice-level handoff for the product build run. It supersedes pass-count summaries that were tied only to earlier planning artifacts.

## Slice: Vault Challenge Authority Consume Metadata Binding

| Field | Value |
| --- | --- |
| Scope | Bound challenge-authority consume requests to submitted subject, context hash, and configured Vault host metadata, in addition to tenant id and challenge id. This closes the local-dev challenge-id-only consume gap before the contract is promoted into production Vault admission flows. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected every `ChallengeConsumeRequest` construction site and local-dev consume route body. Pass 2: added submitted metadata fields to the consume request and enforced same-record matching in the in-memory authority before replay/time checks mark a challenge consumed. Pass 3: routed local-dev `/v1/challenge-authority/consume` form fields through the same binding checks and preserved unknown/cross-tenant metadata withholding. Pass 4: added API, file-store, and route tests proving subject/context/Vault-host mismatches fail closed without consuming the challenge, then reran full validation. |
| Risks found | A consume API that accepts only tenant id and challenge id is too weak for the future high-risk/CI trust boundary because a leaked or confused challenge id could be replayed outside the original subject/context/Vault-host intent. Metadata mismatch rejection also has to happen before consumed-state mutation, otherwise a failed attempt could burn a valid challenge. Unknown and cross-tenant attempts must not reveal issued record metadata while reporting failure. |
| Improvements made | `ChallengeConsumeRequest` now carries `submitted_subject`, `submitted_context_hash`, and `submitted_configured_vault_host`. In-memory and file-backed authority consume paths compare those values against the same-tenant issued `ChallengeAuthorityRecord`, return explicit mismatch reason codes, and leave the record unconsumed on mismatch. The local-dev consume route accepts `subject`, `context_hash`, and `vault_host` form values and exercises the same checks through route tests. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api challenge_authority -- --nocapture` passed with 10 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev challenge_authority -- --nocapture` passed with 10 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 366 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Local-dev challenge-authority contract hardening only. No production authn/authz, no production distributed storage, no production audit sink, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, and no OS sandbox mutation. Metadata mismatches fail closed without consuming a challenge, and unknown/cross-tenant consume attempts do not expose record metadata. |
| Open gates | Bind production consume calls to authenticated principals, worker identity, and admitted evidence payloads; decide transaction semantics between challenge store and audit sink; add production replay/retention/tamper-evidence policy; and wire CI/high-risk endpoint flows to use Vault-issued challenges instead of local-only authority state. |
| Next owner/action | Move the challenge-authority contract from local-dev route/file-store semantics into a production Vault admission API with authenticated callers, durable storage, audit writes, and explicit fail-closed outage behavior. |

## Slice: Local-Dev Challenge Authority Audit-Path Routes

| Field | Value |
| --- | --- |
| Scope | Wired minimized challenge-authority audit JSONL writes into local-dev Vault issue/consume routes behind an explicit temp-scoped `audit_path` form parameter. This makes the audit sink behavior testable through the same route boundary as challenge issue/consume without making audit writes mandatory by default. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected current route summaries and the shared `AuditRecord`/`AuditChallengeAuthoritySummary` schema. Pass 2: added `audit_path` validation that requires an absolute path under `std::env::temp_dir()` and rejects empty, relative, parent-dir, and non-temp paths before authority/store mutation. Pass 3: added route audit status fields and JSONL append helpers that create temp parents, write shared audit records, and do not echo paths. Pass 4: added focused route tests for issue/consume audit writes, path non-disclosure, raw nonce absence, and invalid audit-path fail-closed behavior, then reran full validation. |
| Risks found | Audit summaries rendered in responses are not enough for production-shaped audit behavior. Allowing arbitrary audit paths could write outside the sandbox or disclose local paths. If audit path validation happens after issue/consume, invalid audit configuration could still mutate challenge state. |
| Improvements made | Issue/consume routes now accept explicit `audit_path` opt-in, append minimized JSONL records with challenge-authority summaries, report `audit_configured`, `audit_status`, and `audit_reason_codes`, and reject invalid audit paths before store mutation. Route responses and audit records do not contain audit or store paths, and tests assert no raw `proof-nonce-` material is written. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev challenge_authority -- --nocapture` passed with 9 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 363 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Local-dev route opt-in only. No production audit sink, no arbitrary path writes, no path disclosure in route output or audit records, no production authn/authz, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, and no OS sandbox mutation. |
| Open gates | Replace local file audit writes with a production audit sink, add tamper evidence and retention, define CI/high-risk fail-closed behavior for audit write failures, bind audit records to authenticated principals, and coordinate challenge store/audit writes transactionally. |
| Next owner/action | Design production Vault audit sink semantics for challenge issue/consume and decide whether audit failure should abort, quarantine, or require break-glass for CI/high-risk package installs. |

## Slice: Local-Dev Challenge Authority File-Store Routes

| Field | Value |
| --- | --- |
| Scope | Wired `ChallengeAuthorityFileStore` into the local-dev Vault issue/consume routes behind an explicit temp-scoped `store_path` form parameter. This makes durable issue/consume behavior exercisable through the HTTP-shaped route boundary without changing the default in-memory route behavior. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: reviewed the new file-store API and current route defaults, deciding to keep memory as the default and require explicit file-store opt-in. Pass 2: added store-path validation requiring an absolute path under `std::env::temp_dir()` and rejecting empty, relative, and parent-dir paths. Pass 3: wired issue/consume to `ChallengeAuthorityFileStore::{issue_with_outcome,consume_with_outcome}` when `store_path` is present, and added store status fields that do not echo the path. Pass 4: added route tests for file-backed issue/consume/replay, path non-disclosure, and non-temp path rejection, then reran full validation. |
| Risks found | Exposing arbitrary file paths through a local-dev HTTP route could write outside the intended sandbox. Route responses could accidentally leak local filesystem paths. Changing the default route store to file-backed state would introduce mutation into existing tests and smoke paths that expect process-memory-only behavior. |
| Improvements made | File-backed route persistence is explicit and constrained to the temp directory. Responses include `store_mode`, `store_available`, `store_stale_lock_recovered`, and `store_reason_codes`, but never return the `store_path`. Invalid paths fail closed with reason codes before mutation. The default route still uses process memory. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev challenge_authority -- --nocapture` passed with 7 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 361 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Local-dev route opt-in only. No production distributed storage, no arbitrary path writes, no path disclosure in route output, no production authn/authz, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, and no OS sandbox mutation. |
| Open gates | Add an authenticated production storage adapter, production audit writes for issue/consume, route-level authz, durable store retention/GC, tamper-evidence policy, and a decision about fail-closed CI behavior for store/audit outages. |
| Next owner/action | Design the production Vault challenge-authority storage adapter around the file-store record semantics and route-level audit contract, then wire CI/high-risk endpoint flows to use it. |

## Slice: Vault Challenge Authority File Store

| Field | Value |
| --- | --- |
| Scope | Added a local file-backed challenge-authority store to `whoathere-vault-api` so issue/consume state can persist across authority instances without storing full challenge objects or raw nonce material. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the existing provider challenge replay file store and the newer durable-safe `ChallengeAuthorityRecord` model. Pass 2: added `ChallengeAuthorityFileStore`, operation outcomes, schema-backed store encoding/decoding, private temp-file writes, atomic rename, lock-file acquisition, and stale-lock recovery. Pass 3: added store validation that rejects corrupt schemas, duplicate record fields, duplicate challenge ids, invalid fields, and any persisted `raw_nonce_stored=true` marker. Pass 4: added persistence/replay/corruption/stale-lock tests, fixed an initially weak duplicate-id test so it duplicates the serialized record line directly, and reran full validation. |
| Risks found | Process-local authority state cannot support real CI/high-risk workflows. A durable store that serializes full challenge objects or raw nonce material would expand the blast radius of a local file leak. A corrupt or ambiguous state file must fail closed without overwriting evidence. Stale lock recovery needs schema-backed timing rather than blind deletion. |
| Improvements made | The API crate now exposes `ChallengeAuthorityFileStore::issue`, `issue_with_outcome`, `consume`, `consume_with_outcome`, and `records`. The store persists only `ChallengeAuthorityRecord` fields with hex-encoded string data, not raw nonce values. It writes through private temp files and rename, removes locks on drop, recovers stale locks, and keeps unknown/corrupt state fail-closed. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api challenge_authority -- --nocapture` passed with 8 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 359 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Local file-backed API contract only. This is not distributed production Vault storage, not authenticated storage, not a production audit sink, not proof minting, not launch authorization, not package-manager execution, not lifecycle/build/import execution, not public registry/index fetch, and not OS sandbox mutation. The store rejects persisted raw-nonce markers and the tests verify no `proof-nonce-` value is serialized. |
| Open gates | Replace the local file store with authenticated production storage, define multi-node locking/transaction semantics, wire issue/consume audit writes to a production sink, bind consume calls to evidence/admission payloads, add retention/GC/tamper evidence, and decide CI/high-risk fail-closed behavior for store and audit outages. |
| Next owner/action | Wire a local-dev route or CLI option to exercise `ChallengeAuthorityFileStore`, then design the production Vault storage adapter around the same record and validation semantics. |

## Slice: Vault Challenge Authority Audit Summaries And Escaped Route JSON

| Field | Value |
| --- | --- |
| Scope | Added a minimized challenge-authority audit summary schema and exposed it through local-dev Vault issue/consume route responses. The slice also hardened challenge-authority route JSON rendering by escaping request-controlled values. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: reviewed the shared audit JSONL schema and the local-dev challenge-authority issue/consume renderers, finding that route responses had no audit summary contract and rendered request-controlled fields without JSON escaping. Pass 2: added `AuditChallengeAuthoritySummary` and an optional `challenge_authority_summary` field to `AuditRecord` with redaction-safe JSON rendering. Pass 3: extended consume decisions with same-tenant non-sensitive metadata for audit summaries, while unknown and cross-tenant consume attempts withhold record metadata. Pass 4: wired issue/consume route JSON to include `audit_event_id` and `challenge_authority_audit_summary`, escaped request-controlled fields, added focused tests, and reran full validation. |
| Risks found | Challenge issue/consume activity needs an audit contract before production Vault storage/auth is meaningful. A naive audit record can leak raw nonce material, probe destinations, local store paths, or cross-tenant record metadata. Separately, local-dev route JSON included request-controlled fields such as request id, tenant id, subject, context hash, and Vault host without escaping. |
| Improvements made | The shared audit schema now has a dedicated challenge-authority summary containing operation, authority, request id, tenant id, challenge id, status, accepted flag, trusted time-source metadata, TTL, expiry, probe count, nonce presence, raw nonce flags, and reason codes. Issue/consume route responses include a deterministic local-dev audit event id and the summary object. Unknown and cross-tenant consumes return zeroed metadata rather than exposing someone else's issued record. JSON list values and challenge route fields are escaped. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-audit challenge_authority -- --nocapture` passed with 1 test; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api challenge_authority -- --nocapture` passed with 4 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev challenge_authority -- --nocapture` passed with 5 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 355 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Audit schema and local-dev response contract only. No production audit sink, no durable challenge store, no production authn/authz, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no OS sandbox mutation, and no raw nonce storage or return. |
| Open gates | Write challenge-authority audit events to a production sink, add retention and tamper-evidence policy, back `ChallengeAuthorityRecord` with authenticated durable storage, bind consume events to evidence/admission payloads, and decide fail-closed behavior when challenge-authority audit writes fail in CI/high-risk workflows. |
| Next owner/action | Add durable Vault challenge-authority storage and audit-sink write semantics, then wire endpoint/CI high-risk flows to use Vault-issued challenges with auditable issue/consume decisions. |

## Slice: Vault Challenge Authority Durable-Safe Records

| Field | Value |
| --- | --- |
| Scope | Refactored the local-dev Vault challenge authority so authority state is shaped like future durable storage and does not retain full `ProviderVerificationChallenge` values or raw nonce material. `InMemoryChallengeAuthority` now stores `ChallengeAuthorityRecord` snapshots keyed by challenge id, while issue output still produces transient sanitized challenge metadata for callers. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: reviewed the split issue/consume contract and found the in-memory authority still kept full challenge objects even though public decisions suppressed raw nonces. Pass 2: added `ChallengeAuthorityRecord` with tenant id, challenge id, subject, context hash, configured Vault host, probe destinations, validity window, nonce-present flag, raw-nonce-stored flag, and consumed status. Pass 3: changed issue to generate the nonce transiently from a deterministic sequence-bound SHA-256 basis, store only the durable-safe record, and return `raw_nonce_returned=false`. Pass 4: changed consume to validate against stored records, enforce tenant ownership, fail closed for unknown/replay/expired challenges, mark accepted records consumed, and added record-level tests before rerunning full validation. |
| Risks found | A public API can hide raw nonce material while private process state still keeps full challenge objects, which is a poor shape for production persistence and increases accidental debug/audit leakage risk. Future durable storage should not require serializing raw nonce material to preserve one-time challenge state. A record-only consume path must still enforce tenant ownership and single-use semantics. |
| Improvements made | Challenge-authority state now stores durable-safe `ChallengeAuthorityRecord` values with `raw_nonce_stored=false`. Nonce material is created only for transient issue output and is not retained in the authority records. The authority exposes record snapshots for tests/diagnostics, records consumed state after the first valid consume, and keeps replay, unknown challenge, tenant mismatch, and expiry failures closed. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api challenge_authority -- --nocapture` passed with 4 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 353 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Local-dev API/storage-shape refinement only. State is still process-local memory, not production durable storage. No production authn/authz, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no OS sandbox mutation, and no raw nonce storage in authority records. |
| Open gates | Replace process-local records with authenticated durable Vault storage, add trusted-clock issuance, add issue/consume audit events and retention/tamper-evidence policy, bind consume to evidence/admission payloads rather than challenge id alone, and decide how endpoint-local replay stores migrate to Vault-issued challenge authority. |
| Next owner/action | Back `ChallengeAuthorityRecord` with a durable store and audit sink, then wire endpoint/CI high-risk flows to request and consume Vault-issued challenges instead of locally issuing them. |

## Slice: Vault Challenge Authority Local Dev Issue/Consume Routes

| Field | Value |
| --- | --- |
| Scope | Exposed the split challenge-authority contract through stateful local-dev Vault routes. `POST /v1/challenge-authority/issue` returns a sanitized challenge issue decision, and `POST /v1/challenge-authority/consume` consumes a previously issued challenge id through a process-local `InMemoryChallengeAuthority`. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the Vault dev router, route-kind logging, challenge-authority API objects, and the existing combined simulation route. Pass 2: added a process-local `OnceLock<Mutex<InMemoryChallengeAuthority>>`, issue/consume route dispatch, and route-kind labels for sanitized request summaries. Pass 3: added JSON renderers for issue and consume decisions that preserve `raw_nonce_returned=false`, no proof minting, no package execution, and explicit status/exit-code fields. Pass 4: added route tests for issue/consume/replay, unknown challenge id, cross-tenant rejection, and no raw nonce leakage, then reran full validation. |
| Risks found | A split API contract without any route-level exercise would not prove callers can make separate requests against the local-dev Vault boundary. Process-local state can accidentally leak raw nonce material if route rendering exposes internal challenge objects. A consume route must bind challenge ids to tenants before production storage/auth is added. |
| Improvements made | Local-dev Vault now supports separate issue and consume calls in one process, matching the shape expected from a future service. Issue output includes challenge id, subject, context hash, Vault host, probe count, expiry, nonce-present flag, and `raw_nonce_returned=false`. Consume output returns accepted/rejected status, consume status, reason codes, HTTP status, and exit code. Replays and cross-tenant consumes fail closed. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev challenge_authority -- --nocapture` passed with 4 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 353 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Local-dev route boundary only. State is process-local memory, not durable storage. No production authn/authz, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no OS sandbox mutation, and no raw nonce return. |
| Open gates | Add durable storage for issued challenges, expose authenticated production issue/consume endpoints, bind consume to evidence/admission payloads rather than challenge id alone, add audit events and retention policy, and decide how endpoint-local replay stores migrate to Vault-issued challenges. |
| Next owner/action | Back the issue/consume authority with durable store semantics and audit events, then wire endpoint/CI flows to request challenges from Vault rather than locally issuing them. |

## Slice: Vault Challenge Authority Split Issue/Consume Contract

| Field | Value |
| --- | --- |
| Scope | Added split issue/consume API objects and an in-memory authority contract for Vault challenge handling. `whoathere-vault-api` now includes `ChallengeIssueRequest`, `ChallengeIssueDecision`, `ChallengeConsumeRequest`, `ChallengeConsumeDecision`, and `InMemoryChallengeAuthority` so production Vault can evolve from a combined simulation into separate authenticated issue and consume calls. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected how provider challenges, active-probe receipts, and replay guards use challenge ids, probes, context, and nonces. Pass 2: added a stateful in-memory authority with private raw challenge storage, sanitized issue decisions, tenant-bound consume decisions, and one-time replay rejection. Pass 3: added API tests proving issue decisions omit raw nonces, first consume succeeds, replay fails closed, unknown challenge ids fail closed, tenant mismatches fail closed, and expired challenges fail closed. Pass 4: reran full workspace validation and the existing Vault challenge-authority smoke to prove the public dev-route/CLI behavior remained stable. |
| Risks found | A combined issue-and-consume simulation is not a production API shape. Returning full internal challenge objects from an issue call could leak raw nonce material into debug/log/audit paths. A consume API that ignores tenant ownership would make cross-tenant challenge ids reusable. |
| Improvements made | The in-memory authority stores raw challenge material privately while returning only sanitized issue metadata: challenge id, subject, context hash, configured Vault host, probe count, expiry, nonce-present flag, and `raw_nonce_returned=false`. Consume decisions are tenant-bound, return stable accepted/rejected status and reason codes, and preserve single-use semantics through the shared replay guard. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api challenge_authority -- --nocapture` passed with 4 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 351 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; narrowed ASCII scan passed. |
| Safety boundaries | API contract and in-memory model only. No durable storage, no production authn/authz, no network-service endpoint, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no OS sandbox mutation, and no raw nonce returned in public decisions. |
| Open gates | Replace in-memory state with durable Vault storage, add per-tenant authenticated callers, bind consume calls to evidence/admission payloads, add audit events for issue/consume/replay/expiry, define retention and tamper evidence, and wire CI/high-risk endpoint flows to use Vault-issued challenge ids. |
| Next owner/action | Implement a local-dev route or service boundary for separate `/issue` and `/consume` calls, then back it with durable storage semantics rather than process-local memory. |

## Slice: Vault Challenge Authority API Contract

| Field | Value |
| --- | --- |
| Scope | Promoted the local-dev challenge-authority behavior into a typed `whoathere-vault-api` contract. The API crate now owns `ChallengeAuthorityRequest`, `ChallengeAuthorityScenario`, `ChallengeAuthorityDecision`, `ChallengeAuthorityStatus`, authority/time-source constants, and `plan_challenge_authority(...)`. The Vault dev route delegates issue/consume/replay behavior to that contract and only renders the decision. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected `whoathere-vault-api`, the dev route, CLI wrapper, dependency graph, and sandbox challenge primitives. Pass 2: added a direct `whoathere-vault-api -> whoathere-sandbox` dependency after confirming no dependency cycle, so the API contract reuses the same provider challenge/replay semantics as endpoint admission. Pass 3: added typed request/scenario/decision/status objects and API tests proving first-consume acceptance plus replay, unknown, tamper, and expiry rejection without raw nonce exposure. Pass 4: refactored `whoathere-vault-dev` to call `plan_challenge_authority(...)`, kept the external JSON/CLI smoke contract stable, removed the direct dev-crate sandbox dependency, and reran focused/full validation. |
| Risks found | Keeping challenge-authority behavior only inside the dev router would make the production Vault implementation likely to drift from the tested local-dev behavior. Duplicating replay logic in Vault dev would also risk divergence from endpoint replay admission. A direct API dependency on sandbox needed an explicit cycle check before adding it. |
| Improvements made | Challenge authority now has a stable typed contract with request id, tenant id, subject, context hash, configured Vault host, Vault-owned timestamp, TTL, scenario flags, authority identity, trusted time-source label, no-authorization booleans, challenge ids, consume statuses, reason codes, HTTP status, and CLI exit code. Dev JSON rendering is derived from `ChallengeAuthorityDecision`, while the one-time consume and rejection semantics remain shared through `ProviderChallengeReplayGuard`. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api challenge_authority -- --nocapture` passed with 2 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev challenge_authority -- --nocapture` passed with 2 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli vault_challenge_simulation -- --nocapture` passed with 2 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 349 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh` passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Contract/refactor only. No production persistent authority, no network-service authentication, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no OS sandbox mutation, and no raw nonce return. |
| Open gates | Implement durable Vault challenge storage, split production issue and consume APIs, add tenant/authn/authz enforcement, bind challenge consumption to admitted evidence/proof workflows, add audit retention/tamper evidence, and decide which endpoint flows must use Vault authority instead of local file-backed issue state. |
| Next owner/action | Build the production-style Vault challenge authority service boundary: durable issue/consume state, authenticated caller model, audit events, and migration wiring from endpoint-local replay stores to Vault-issued challenges for CI/high-risk installs. |

## Slice: Vault Challenge Authority Simulation

| Field | Value |
| --- | --- |
| Scope | Added a local-dev Vault challenge-authority simulation route and CLI wrapper to prove the server-side issue/consume/replay contract that should replace local file-backed challenge authority for CI and high-risk workflows. `POST /v1/challenge-authority-simulations` issues a nonce-bound provider challenge from a Vault-owned clock, consumes it exactly once, and can simulate replay, unknown challenge, context mutation, and expiry. `whoathere vault challenge-sim` exposes the same contract with fail-closed process exit codes. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the existing Vault dev router, CLI Vault command surface, replay guard primitives, and recent local replay-store gaps. Pass 2: reused `ProviderChallengeReplayGuard` in `whoathere-vault-dev` instead of creating a second challenge model, added the local-dev authority route, and marked server-clock ownership, no raw nonce return, and non-authorization semantics in the JSON contract. Pass 3: added CLI parsing/rendering for `vault challenge-sim [--replay|--unknown-challenge|--mutate-context|--expired]`; the first smoke found fail-closed route JSON still returned process exit 0, so the wrapper now emits a top-level `exit_code=` line and denied scenarios return exit 20. Pass 4: added focused Vault dev and CLI tests plus `scripts/whoathere-vault-challenge-authority-smoke.sh` covering accepted, replayed, unknown, tampered, and expired challenge scenarios. |
| Risks found | CI/high-risk challenge authority cannot safely depend on local endpoint file state as the long-term trust boundary. A fake dev route could accidentally look like authorization or proof minting unless it explicitly reports `authorization=false`, `proof_minted=false`, and `execution_allowed=false`. CLI wrappers around HTTP-style JSON can accidentally hide fail-closed verdicts behind process exit 0 if they do not expose a top-level exit-code contract. Raw challenge nonces must not be returned in diagnostics or audit-like output. |
| Improvements made | The Vault dev router now has a challenge-authority simulation route with `authority=vault_challenge_authority.v1`, `trusted_time_source=vault_server_clock`, `client_time_accepted=false`, `raw_nonce_returned=false`, TTL, challenge ids, consume statuses, scenario flags, reason codes, and exit code. Replay, unknown, mutated, and expired submissions all return HTTP 409 and exit 20 through the CLI wrapper. The route uses the same sandbox replay guard semantics as endpoint admission, preserving one-time consume, unknown challenge rejection, mutation detection, expiry rejection, and nonce secrecy. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev challenge_authority -- --nocapture` passed with 2 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli vault_challenge_simulation -- --nocapture` passed with 2 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 347 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-vault-challenge-authority-smoke.sh`, provider challenge smoke, Linux active-probe admission smoke, and replay-store stress smoke passed; targeted smoke shell syntax checks passed; narrowed ASCII scan passed. |
| Safety boundaries | Local-dev simulation only. No production distributed store, no network service authentication, no proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no OS sandbox mutation, and no raw nonce return. The route models the server-side authority contract that production Vault still needs to implement with durable storage, authenticated callers, audit, retention, and trusted clock behavior. |
| Open gates | Implement real Vault-backed durable challenge issuance/consume APIs, authenticated admission callers, cross-runner replay prevention, production audit retention/tamper evidence, clock-source policy, and rollout rules for when endpoint-local replay state is acceptable versus when Vault authority is mandatory. |
| Next owner/action | Turn the simulated contract into a production Vault API design and implementation slice, then wire CI/high-risk endpoint flows to request/consume Vault-issued challenges instead of local file-backed issue authority. |

## Slice: Replay-Store Audit And Separate-Process Stress

| Field | Value |
| --- | --- |
| Scope | Added minimized audit records for Docker active-probe replay-store admission and a separate-process stress smoke for the local file-backed replay store. `evidence linux-active-probe-docker --admit` now accepts `--audit-path <path>` and honors `WHOATHERE_AUDIT_PATH`, records replay-store issue/consume/failure outcomes in JSONL, and keeps audit records free of replay-store paths, raw challenge nonces, raw Docker logs, and raw evidence payloads. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected existing launch/protect audit JSONL patterns, Docker replay-store admission output, and local replay-store outcome gaps. Pass 2: extended `whoathere-audit` with `AuditReplayStoreSummary` and added replay-store audit serialization tests proving no raw nonce or path is recorded. Pass 3: added outcome-aware replay-store issue/consume APIs so callers can audit stale-lock recovery without changing existing issue/consume call sites. Pass 4: wired Docker admission audit status into text/JSON output, added audit tests for successful consume and corrupt-store issue failure, then added `scripts/whoathere-replay-store-stress-smoke.sh` to run 12 independent CLI processes against one shared replay-store file. |
| Risks found | Replay-store operations were previously visible only through command output and local store state, making issue/consume/failure outcomes hard to audit. A naive audit record could leak local paths, raw challenge nonces, or raw probe evidence. Thread-level concurrency coverage did not prove the actual CLI process boundary. |
| Improvements made | Audit JSONL now includes a `replay_store_summary` with configured flag, operation, availability, stale-lock recovery flag, challenge id, replay status, accepted flag, and reason codes. Docker admission output includes audit write status without changing authorization semantics. Store issue/consume expose outcome-aware variants while preserving existing APIs. The new stress smoke builds the CLI, launches 12 separate `whoathere evidence linux-active-probe-docker --admit --replay-store ... --audit-path ...` processes, verifies every process fails closed without proof minting or execution, verifies all store records are consumed, verifies no lock remains, and verifies each audit record is minimized. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-audit replay_store -- --nocapture` passed with 1 test; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox provider_challenge_file_store -- --nocapture` passed with 4 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_linux_active_probe_docker -- --nocapture` passed with 8 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 342 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed after refactoring the audit writer into a request struct; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; `scripts/whoathere-replay-store-stress-smoke.sh` passed with 12 workers; provider challenge, fixture admission, fixture active-probe, provider, endpoint, compatibility, Docker active-probe, Docker replay-admission, and internal-Vault smokes passed; `bash -n` over smoke/build scripts passed. |
| Safety boundaries | Audit and stress diagnostics only. No proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no raw nonce persistence, no raw evidence/log capture, no shared audit-file append contention assumption, and no distributed replay authority. The stress smoke uses per-worker audit files while sharing only the replay-store file. |
| Open gates | Source challenge time from a trusted clock, decide whether CI challenge authority should be Vault-backed instead of local file-backed, add production audit retention/tamper-evidence/admin policy, and decide whether production needs locked append or queue-backed audit writes for many writers to one audit sink. |
| Next owner/action | Move from local replay-store hardening to the next proof gap: either trusted-clock/Vault-backed challenge authority or a Docker/Linux provider path that can satisfy remapped user namespace evidence before any admitted receipt can become trusted proof material. |

## Slice: Replay-Store Stale Lock And Concurrency Hardening

| Field | Value |
| --- | --- |
| Scope | Hardened the local file-backed provider-challenge replay store against abandoned lock files and concurrent issue/consume races. Store operations now acquire a schema-backed sidecar lock with creation time and pid metadata, recover stale locks after the store TTL, fall back to filesystem mtime for legacy or malformed lock files, and keep issue/consume/prune/snapshot operations serialized through the same acquisition path. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: reviewed replay-store issue/consume/snapshot locking, Docker admission use, endpoint env defaults, and the remaining stale-lock/concurrency gates from prior slices. Pass 2: changed lock creation to write `whoathere.provider_challenge_replay_lock.v1`, `created_at_unix_seconds`, and `pid`, and made failed lock metadata writes remove the just-created lock before returning the error. Pass 3: added stale-lock recovery using logical challenge time for schema-valid locks and filesystem mtime/current wall time for malformed or legacy lock files, while preserving fail-closed `WouldBlock` behavior for fresh locks. Pass 4: added focused stale-lock recovery and threaded concurrent issue/consume tests using separate store handles, then reran full Rust gates and local/Docker smokes. |
| Risks found | A stale lock left by a killed process could permanently deny all future replay-store operations. A malformed or legacy lock file without creation metadata could also become unrecoverable. Concurrent issue/consume operations could lose records or double-consume challenges if sidecar locking and atomic rewrite behavior were not exercised under contention. |
| Improvements made | `ProviderChallengeReplayFileStore` now routes `issue`, `consume`, `prune_expired`, and `snapshots` through a common lock-acquisition helper. Lock files carry schema, timestamp, and process id metadata. Stale schema-valid locks are removed once `now_unix_seconds - created_at_unix_seconds` exceeds the store TTL. Legacy/malformed lock files can be recovered by mtime age. The new concurrency test starts eight threads with independent store handles, issues and consumes unique challenges, then verifies every persisted snapshot is consumed and the sidecar lock is gone. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox provider_challenge_file_store -- --nocapture` passed with 4 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_linux_active_probe_docker -- --nocapture` passed with 6 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 340 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; provider challenge, fixture admission, fixture active-probe, provider, endpoint, compatibility, Docker active-probe, Docker replay-admission, and internal-Vault smokes passed; `bash -n` over smoke/build scripts passed; narrowed ASCII scan passed; Docker cleanup checks showed only the pre-existing BuildKit builder container running, only default Docker networks present, and expected local images present. |
| Safety boundaries | Local replay-store hardening only. No proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no distributed replay authority, and no change to provider trust. Stale-lock recovery uses caller-supplied logical challenge time for schema-valid locks, so production still needs a trusted time source before CI relies on local file state. |
| Open gates | Decide whether CI challenge authority should be Vault-backed instead of local file-backed, source challenge time from a trusted clock, and define production audit retention/tamper-evidence/admin policy. |
| Next owner/action | Decide whether the local file store remains endpoint-only or is replaced by a Vault-backed challenge service for CI/high-risk workflows. |

## Slice: Replay-Store Endpoint Defaults

| Field | Value |
| --- | --- |
| Scope | Added endpoint/default wiring for replay-store configuration through `WHOATHERE_REPLAY_STORE`. Endpoint setup now accepts and exports an optional replay-store path, shim manifest guidance lists the replay-store environment default, and Docker active-probe admission uses the env default only when `--admit` is active and no explicit `--replay-store` flag is supplied. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected endpoint setup env defaults, shim manifest output, Docker admission parsing, and existing replay-store CLI behavior. Pass 2: added `replay_store` to endpoint setup parsing/rendering, CLI help, and export guidance while preserving existing required fields. Pass 3: added an env-aware Docker active-probe parser that applies `WHOATHERE_REPLAY_STORE` only in admission mode, leaves non-admission diagnostics unaffected by the env var, and lets explicit `--replay-store` override the env default. Pass 4: updated endpoint and Docker admission tests plus endpoint/Docker smokes to prove the env default path is surfaced and used without enabling proof minting or execution. |
| Risks found | A global replay-store env var could accidentally break non-admission diagnostics if it were applied unconditionally. Endpoint setup could also advertise an env var that no command actually consumes. Both were avoided by applying the env default only to Docker admission mode and validating endpoint output plus Docker smoke behavior. |
| Improvements made | `endpoint setup` now renders `replay_store=...` and `export_WHOATHERE_REPLAY_STORE=...` when configured. `shim install --dry-run` reports `WHOATHERE_REPLAY_STORE` in the env-default contract. `evidence linux-active-probe-docker --admit` can use `WHOATHERE_REPLAY_STORE` without a CLI flag, while non-admission Docker evidence ignores that env default. The Docker replay-admission smoke now uses the env default for its durable-store path. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli endpoint_setup -- --nocapture` passed with 3 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_linux_active_probe_docker -- --nocapture` passed with 6 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 338 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; provider challenge, fixture admission, fixture active-probe, provider, endpoint, compatibility, Docker replay-admission, Docker active-probe, and internal-Vault smokes passed; `scripts/whoathere-endpoint-smoke.sh` passed with replay-store export assertions; `scripts/whoathere-linux-active-probe-docker-admission-smoke.sh` passed using `WHOATHERE_REPLAY_STORE` for the durable-store path; `bash -n` over smoke/build scripts passed; narrowed ASCII scan passed; Docker cleanup checks showed only the pre-existing BuildKit builder container running, only default Docker networks present, and expected local images present. |
| Safety boundaries | Configuration/export wiring only. No proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, and no automatic use of replay-store state by non-admission diagnostics. The endpoint setup path remains fail-closed because local providers still cannot verify high-risk execution. |
| Open gates | Add production path policy for where replay state should live, decide local file versus Vault-backed challenge authority for CI, add trusted clock, and define production audit sink behavior. |
| Next owner/action | Decide whether local endpoint replay state remains a dev/standalone feature or moves behind Vault-backed challenge issuance for CI. |

## Slice: Docker Admission Durable Replay-Store Wiring

| Field | Value |
| --- | --- |
| Scope | Wired the local file-backed replay store into `whoathere evidence linux-active-probe-docker --admit` through an explicit `--replay-store <path>` option. Docker admission can now issue and consume replay-owned challenges through durable local state while preserving fail-closed proof and execution gates. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the Docker admission renderer, in-memory replay guard use, file-store API, existing Docker admission smoke, and JSON/text output contracts. Pass 2: added `admit_linux_active_probe_receipt_with_replay_decision` so replay decisions produced by a durable store can feed the same admission result object as the in-memory guard. Pass 3: added CLI parsing, help text, JSON/text `replay_store` status output, admission-only validation for `--replay-store`, store-backed issue/consume/preconsume logic, and fail-closed handling for corrupt, locked, permission-denied, or otherwise unavailable stores. Pass 4: added unit tests for store-backed Docker admission, repeated CLI invocations appending consumed records, replay rejection, corrupt-store fail-closed behavior, and misuse without `--admit`; extended the Docker admission smoke to exercise `--replay-store` on the real container path and inspect the resulting store file. |
| Risks found | Durable replay-state errors must not be treated as CLI misuse once admission is requested because corruption or lock contention is an enforcement failure that should deny, not silently fall back to in-memory state. `--replay-store` without `--admit` would otherwise create misleading state for a command that is not performing admission. Store-backed admission must not expose the raw challenge nonce in JSON or file contents. |
| Improvements made | `evidence linux-active-probe-docker --admit --replay-store <path>` now issues and consumes through `ProviderChallengeReplayFileStore`. Output includes whether the replay store was configured, which operation was attempted, availability, and store reason codes. Corrupt store state returns exit 20 with fail-closed replay-store reason codes and does not overwrite the corrupt file. The real Docker smoke verifies the store contains schema plus `challenge_nonce_digest=`, contains no `proof-nonce-`, records a consumed challenge, and is private on Unix. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_linux_active_probe_docker -- --nocapture` passed with 5 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox provider_challenge_file_store -- --nocapture` passed with 2 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 337 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; provider challenge, fixture admission, fixture active-probe, provider, endpoint, compatibility, Docker replay-admission, Docker active-probe, and internal-Vault smokes passed; `scripts/whoathere-linux-active-probe-docker-admission-smoke.sh` specifically passed no-network, durable replay-store, replay, and internal-Vault Docker paths, all failing closed without proof minting or execution authorization; `bash -n` over smoke/build scripts passed; narrowed ASCII scan passed; Docker cleanup checks showed only the pre-existing BuildKit builder container running, only default Docker networks present, and expected local images present. |
| Safety boundaries | Diagnostic/admission wiring only. No proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no raw nonce persistence, and no fallback from durable-store failure to in-memory approval. Store-backed accepted replay ownership is still admission evidence only; the local Docker receipt remains unsatisfied because user namespace proof is not available. |
| Open gates | Decide local file versus Vault-backed challenge authority for CI, add trusted clock handling, add audit events for issue/consume/recovery outcomes, and prove concurrency behavior under separate process pressure before relying on local replay state for production. |
| Next owner/action | Decide whether local replay-store admission remains endpoint-only or is replaced by a Vault-backed challenge service. |

## Slice: File-Backed Replay Store

| Field | Value |
| --- | --- |
| Scope | Added a local durable replay-store boundary for provider challenge replay state. The store persists snapshot records to a private file, uses a sidecar lock file for issue/consume/prune/snapshot operations, writes through a private temp file plus rename, stores challenge nonce digests rather than raw nonces, and keeps single-use consume state across store instances. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the replay guard snapshot API, nonce-digest binding, active-probe admission callers, and durable-state requirements. Pass 2: added `ProviderChallengeReplayFileStore` with explicit `issue`, `consume`, `prune_expired`, and `snapshots` operations guarded by a lock sidecar. Pass 3: added a line-based hex-encoded schema that restores only snapshot material, rejects corrupt state with `InvalidData`, creates files with private permissions, and avoids persisting raw challenge nonces. Pass 4: hardened parser handling for duplicate record fields and duplicate challenge IDs, made temp test roots unique, and added focused tamper/corruption/single-use persistence tests. |
| Risks found | A durable replay store can become replay oracle state if raw nonces are persisted, if corrupt files are auto-repaired, or if duplicate fields/records are accepted ambiguously. A file-backed lock is not a distributed challenge service and should not be mistaken for production multi-host coordination. Atomic rename behavior is targeted at the current macOS/Linux endpoint scope; Windows remains a later expansion track. |
| Improvements made | Replay challenge issue/consume state now survives process/store re-open boundaries. Replayed challenges remain rejected after the accepted consume is persisted. The store file contains `challenge_nonce_digest=` but not the raw challenge nonce. Corrupt state fails closed without overwriting the corrupt file. Duplicate record fields and duplicate challenge IDs are rejected instead of being silently overwritten. Lock files are removed after operations, and store/lock/temp files are created with private Unix permissions. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox provider_challenge_file_store -- --nocapture` passed with 2 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox provider_challenge_store_rejects -- --nocapture` passed with 2 tests; focused replay-guard and active-probe-admission tests passed with 4 and 2 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 335 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; fixture admission, provider challenge, fixture active-probe, provider, endpoint, compatibility, Docker replay-admission, Docker active-probe, and internal-Vault smokes passed; `bash -n` over all smoke/build scripts passed; narrowed ASCII scan over `README.md`, `docs/product-build-run`, `whoathere`, and `scripts` passed; Docker cleanup checks showed only the pre-existing BuildKit builder container running, only default Docker networks present, and expected local images present. |
| Safety boundaries | Durable local replay state only. No proof minting, no launch authorization, no package-manager execution, no lifecycle/build/import execution, no public registry/index fetch, no distributed replay service, no trusted clock, no real Vault auth, and no change to Docker/provider admission trust. The store is a local endpoint building block, not production challenge authority. |
| Open gates | Add trusted time handling, decide local versus Vault-backed replay authority for CI, add audit correlation for issue/consume/recovery outcomes, and design a distributed replay service before multi-runner or multi-host challenge issuance relies on this state. |
| Next owner/action | Move the explicit Docker admission replay-store path into configured endpoint/provider defaults or replace local persistence with a Vault-backed challenge service. |

## Slice: Persistable Replay Guard Snapshots

| Field | Value |
| --- | --- |
| Scope | Added a persistable snapshot/restore boundary for provider challenge replay state while changing replay records to retain a nonce digest instead of the raw challenge nonce. This prepares durable challenge storage without enabling proofs or execution. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: inspected `ProviderChallengeReplayGuard`, challenge nonce/id construction, mutation checks, existing admission callers, and the open durable-replay gate. Pass 2: replaced internal raw nonce retention with `provider_challenge_nonce_digest`, preserving nonce-mismatch detection by hashing submitted challenge nonces during consume. Pass 3: added `ProviderChallengeReplaySnapshot`, `snapshots()`, and `restore(...)`, then added focused tests proving restored guards accept original challenges once, reject replay, preserve consumed state, and reject mutated nonces without storing the raw nonce. |
| Risks found | Persisting raw nonces would make the future durable store more sensitive than necessary. Persisting only challenge ids would be insufficient because mutation checks need subject, context, Vault host, probe set, timing, expiry, and nonce binding. A snapshot API could be misread as a trusted store unless it remains clearly separate from file locking, trusted clocks, and proof authorization. |
| Improvements made | Replay records now compare nonce digests instead of raw nonce strings. The snapshot model contains all fields needed to restore replay/mutation decisions, including consumed status, but not the raw nonce. Restored guards preserve single-use semantics and mutation rejection across process-state boundaries in tests. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox provider_challenge_replay_guard -- --nocapture` passed with 4 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox linux_active_probe_admission -- --nocapture` passed with 2 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli linux_active_probe_docker -- --nocapture` passed with 7 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 331 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; fixture admission, provider challenge, and Docker replay-admission smokes passed; `bash -n` over all smoke/build scripts passed; narrowed ASCII scan over `README.md`, `docs/product-build-run`, `whoathere`, and `scripts` passed; Docker cleanup checks showed only the pre-existing BuildKit builder container running, only default Docker networks present, and expected local images present. |
| Safety boundaries | Replay-state structure only. No file store, no OS locks, no trusted clock, no npm/pip execution, no lifecycle/build/import execution, no Docker execution changes, no public network access, no proof minting, and no launch authorization change. Snapshots are not proof material by themselves. |
| Open gates | Add an authenticated/permissioned local or Vault-backed durable store, atomic issue/consume operations, trusted time source, retention/GC policy, and audit correlation before replay snapshots can become a production challenge service. |
| Next owner/action | Build a file-backed or Vault-backed replay store using these snapshots, with atomic consume semantics and strict permissions, then wire it into Docker/provider admission flows. |

## Slice: Docker Active Probe Replay Admission

| Field | Value |
| --- | --- |
| Scope | Wired real Docker Linux active-probe receipts into replay-owned admission mode with `evidence linux-active-probe-docker --admit`, so the container-emitted receipt can be consumed by `ProviderChallengeReplayGuard` without becoming a trusted proof or enabling package-manager execution. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the Docker active-probe renderer, challenge creation path, receipt parsing, previous fixture-only admission command, and Docker internal-Vault smoke. Pass 2: added `--admit` and `--replay` to `evidence linux-active-probe-docker`, refactored the renderer into a request struct, and made admission mode issue a replay-guard-owned challenge before running or planning the Docker probe. Pass 3: rendered `admission_applied`, optional `preconsume`, and `admission` in Docker JSON/text output while preserving `authorization=false`, `proof_minted=false`, and `execution_allowed=false`. Pass 4: added CLI regression coverage plus `scripts/whoathere-linux-active-probe-docker-admission-smoke.sh`, covering no-network real Docker output, replay rejection, and internal-Vault real Docker output. |
| Risks found | A real Docker receipt path could otherwise bypass the replay-owned admission check that was proven only with fixtures. Replay testing should not require Docker execution, but real executed cases must still show the container user, image contract, internal-network verification, and receipt failure reasons. A Docker receipt with accepted replay ownership must not be confused with proof minting because the local runtime still lacks remapped current-process user namespace evidence. |
| Improvements made | Docker active-probe output now distinguishes raw receipt validation from admission ownership. `--admit` creates a nonce/expiry-bound challenge through the replay guard, runs or plans the Docker probe against that challenge, consumes the challenge once, and reports whether rejection came from replay ownership, receipt content, or Docker preflight/runtime diagnostics. `--replay` is rejected as misuse unless admission mode is enabled. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli linux_active_probe_docker -- --nocapture` passed with 7 tests; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 329 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; admission, fixture, provider challenge, provider readiness, endpoint, and compatibility smokes passed; `scripts/whoathere-build-linux-active-probe-image.sh` rebuilt `whoathere/linux-active-probe:local`; Docker active-probe, Docker replay-admission, and internal-Vault smokes passed; `bash -n` over all smoke/build scripts passed; narrowed ASCII scan over `README.md`, `docs/product-build-run`, `whoathere`, and `scripts` passed; Docker cleanup checks showed only the pre-existing BuildKit builder container running, only default Docker networks present, and expected local images present. |
| Safety boundaries | Docker admission mode still does not run npm or pip, lifecycle/build/import code, public registry/index fetches, or dynamic detonation. The replay path can be exercised without Docker execution. The executed smoke uses only the local active-probe image, an ephemeral container, and a throwaway Docker internal network with an inert Vault fixture. Accepted replay ownership is admission evidence only and still reports no proof minting or execution authorization. |
| Open gates | Persist replay-guard state across CLI/process boundaries with a trusted clock, add local probe image provenance/signing verification, obtain a runtime-satisfied remapped user namespace proof, and define the disabled proof-mapping policy that can later convert an admitted satisfying receipt into trusted containment/egress proofs. |
| Next owner/action | Move from in-command replay guard simulation to durable challenge issuance/consumption, then add image provenance checks before any admitted Docker receipt can influence launch proof material. |

## Slice: Replay-Owned Active Probe Admission

| Field | Value |
| --- | --- |
| Scope | Added a replay-owned admission gate for Linux active-probe receipts so a satisfying receipt is not considered admissible unless it is bound to a challenge issued by the replay guard and consumed exactly once. This still does not mint trusted proofs or authorize package-manager execution. |
| Pass count | 5 |
| Pass artifacts/evidence | Pass 1: inspected `ProviderChallengeReplayGuard`, challenge mutation checks, `ProviderChallengeUseDecision`, receipt parsing, and existing fixture/Docker receipt flows. Pass 2: added `LinuxActiveProbeAdmission` plus `admit_linux_active_probe_receipt`, which consumes the challenge through the replay guard, parses the receipt against the submitted challenge, merges replay and receipt reason codes, and accepts only when both replay and receipt validation succeed. Pass 3: added sandbox tests for accepted admission, replay rejection, unknown challenge rejection, and invalid receipt rejection. Pass 4: added `whoathere evidence linux-active-probe-admission --subject <id> --context-hash <hash> --vault-host <host> --profile complete|incomplete|overpermissive [--replay|--unknown-challenge|--mutate-context] [--json]`, with JSON/text output showing issued challenge, submitted challenge, optional preconsume, admission decision, and receipt. Pass 5: added CLI tests and `scripts/whoathere-linux-active-probe-admission-smoke.sh` covering accepted, replayed, unknown, mutated-context, and incomplete receipt scenarios. |
| Risks found | A complete active-probe receipt is not sufficient by itself; without replay ownership it can be copied, replayed, or rebound to another launch context. Consuming only after a perfect receipt would allow attackers to retry with the same challenge after a failed attempt, so the admission path consumes the challenge when a receipt is submitted. A successful admission can be misread as execution authorization unless the command explicitly reports `authorization=false`, `proof_minted=false`, and `execution_allowed=false`. |
| Improvements made | The sandbox crate now has an explicit admission result object. The CLI can simulate receipt admission independent of Docker and demonstrates that replayed, unknown, mutated-context, and invalid receipts fail closed. JSON output distinguishes `issued_challenge`, `submitted_challenge`, `preconsume`, `admission`, and `active_probe_receipt`, making it clear whether a rejection came from replay ownership or receipt content. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox linux_active_probe_admission -- --nocapture` passed with 2 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_linux_active_probe_admission -- --nocapture` passed with 3 tests; `scripts/whoathere-linux-active-probe-admission-smoke.sh` passed accepted/replay/unknown/mutated/incomplete command-line scenarios; clippy caught an 8-argument CLI renderer and the code was refactored to a request struct rather than suppressing the lint; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 328 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings` passed; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check` passed; admission, fixture, provider challenge, provider readiness, endpoint, and compatibility smokes passed; `scripts/whoathere-build-linux-active-probe-image.sh` rebuilt `whoathere/linux-active-probe:local`; Docker active-probe and internal-Vault smokes passed; `bash -n` over all smoke/build scripts passed; narrowed ASCII scan over `README.md`, `docs/product-build-run`, `whoathere`, and `scripts` passed; Docker cleanup checks showed only the pre-existing BuildKit builder container running, only default Docker networks present, and expected local images present. |
| Safety boundaries | Fixture/admission simulation only. No Docker execution is required, no package manager ran through protected execution, no lifecycle/build/import code executed, no public registry/index fetch occurred, no OS/network mutation occurred, no trusted proof was minted, and launch authorization did not change. |
| Open gates | Wire real Docker/Linux active-probe receipts into this admission path, persist replay-guard state across process boundaries with a trusted clock, add image provenance/signing verification, and only then map admitted satisfying receipts into trusted containment/egress proofs. |
| Next owner/action | Connect Docker active-probe execution to replay-owned admission output, then define the proof-mapping policy that remains disabled until user namespace, provenance, and persistence gates are satisfied. |

## Slice: Non-Root User Namespace Evidence

| Field | Value |
| --- | --- |
| Scope | Improved the Docker Linux active-probe user isolation evidence by running the probe as a non-root container user, recording UID/GID maps, recording nested user namespace attempt results, and verifying that only real remapped UID/GID maps can satisfy the receipt contract. |
| Pass count | 5 |
| Pass artifacts/evidence | Pass 1: probed OrbStack Docker security options, default container UID/GID maps, `--userns=private`, `--userns=auto`, and nested `unshare -Ur` behavior under hardened flags. Pass 2: confirmed the default Docker namespace is a full identity map (`0:0:4294967295`), Docker rejects `--userns=private|auto`, and nested unshare is blocked when seccomp remains enabled. Pass 3: confirmed disabling seccomp permits nested userns creation, and running as `65532:65532` can create a non-root nested map only when seccomp is unconfined; rejected that path as a proof because it weakens another required control. Pass 4: changed the active probe to run as Docker user `65532:65532`, emit `probe.uid`, `probe.gid`, `probe.uid_map`, `probe.gid_map`, `probe.userns_unshare`, nested map facts when available, and equivalent inline fallback facts. Pass 5: extended the receipt model and JSON/text rendering with user namespace evidence fields; changed Docker evidence derivation so `user_namespace.isolated=true` only when the current process UID and GID maps are remapped away from host root; added focused tests and smoke assertions. |
| Risks found | Running as container root was an avoidable local-risk default even though package execution is still disabled. A nested child user namespace is not equivalent to the active probe process being isolated. Disabling seccomp to permit `unshare` creates a false-positive path because the receipt also requires seccomp enforcement. OrbStack's current Docker daemon does not expose daemon-level userns remap in `SecurityOptions`, and Docker rejects the private/auto modes needed to satisfy the proof locally. |
| Improvements made | The Docker executor now passes `--user 65532:65532`; the probe records current UID/GID, current UID/GID maps, nested-userns attempt status, and nested maps if present; receipt JSON exposes `user_namespace_uid`, `user_namespace_gid`, `user_namespace_uid_map`, `user_namespace_gid_map`, `nested_user_namespace_attempt`, `nested_user_namespace_created`, and nested maps; unit tests prove remapped maps can satisfy the receipt contract, while the local OrbStack path remains fail-closed with `linux_active_probe_user_namespace_not_isolated`. |
| Validation | `docker info --format '{{json .}}'` showed OrbStack security options include seccomp and cgroupns but not userns-remap; direct default container probe showed UID/GID maps `0:0:4294967295`; `docker run --userns=private` and `--userns=auto` failed with invalid USER mode; hardened nested `unshare -Ur` failed with operation not permitted; seccomp-unconfined nested unshare succeeded and was rejected as proof-weakening; non-root seccomp-unconfined nested unshare showed a `0:65532:1` nested map but still required seccomp unconfined; `docker run --user 65532:65532 ... /usr/local/bin/whoathere-active-probe` succeeded; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli linux_active_probe_docker -- --nocapture` passed with 6 tests; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox linux_active_probe -- --nocapture` passed; `scripts/whoathere-build-linux-active-probe-image.sh` rebuilt `whoathere/linux-active-probe:local`; direct Docker-backed evidence command returned expected exit 20 with `container_user=65532:65532`, identity UID/GID maps, and nested unshare denied; `scripts/whoathere-linux-active-probe-docker-smoke.sh` and `scripts/whoathere-linux-active-probe-internal-vault-smoke.sh` passed; direct `--docker-network bridge` run returned expected exit 20 with `docker_invoked=false`; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 323 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; fixture, provider challenge, provider readiness, endpoint, and compatibility smokes; `bash -n scripts/*.sh`; ASCII scan; `docker ps --format ...` confirmed no active-probe containers were left behind; `docker image ls --format ...` confirmed both the BuildKit base image and local WhoaThere probe image were present. |
| Safety boundaries | The probe still runs without package-manager execution, lifecycle/build/import execution, proof minting, or launch authorization. The only Docker behavior change is a non-root container user plus additional read-only `/proc` evidence collection. The implementation does not disable seccomp, does not request privileged Docker mode, does not rely on `--userns` modes unsupported by the local runtime, and does not treat nested child namespace creation as active-process isolation. |
| Open gates | A production-satisfying user namespace proof still requires Docker daemon userns-remap, rootless/userns-capable Linux runtime support, or a dedicated Linux sandbox runner that can launch the active probe inside a remapped user namespace while preserving seccomp and cgroup evidence. Replay-guard ownership and image provenance/signing remain required before any satisfied receipt can become trusted proof material. |
| Next owner/action | Implement or detect a Linux runtime profile that provides a real remapped current-process UID/GID map under seccomp, then rerun the same receipt contract without changing launch authorization until replay-guard and provenance gates are complete. |

## Slice: Internal-Network Vault Allowance Probe

| Field | Value |
| --- | --- |
| Scope | Added the first controlled configured-Vault allow path for the Docker Linux active-probe executor by allowing an explicit Docker internal network, probing only the configured Vault fixture, and continuing to fail closed without trusted proof minting or launch authorization. |
| Pass count | 5 |
| Pass artifacts/evidence | Pass 1: inspected the active-probe CLI, receipt contract, probe image script, smoke harnesses, and local image tools; confirmed the image has `nc`, `wget`, BusyBox, `awk`, and `sh`. Pass 2: validated a Docker internal-network fixture pattern using a unique `--internal` network, a network alias, and an inert `nc` HTTP fixture. Pass 3: added `--docker-network <internal-network>` to `evidence linux-active-probe-docker`, required Docker network inspection to report `Internal=true` before attaching, passed only the configured Vault host into the probe, and rendered `network_internal_verified`, `configured_vault_probe_attempted`, and `configured_vault_probe_allowed`. Pass 4: updated the local probe image contract and inline fallback to emit `probe.vault_connect=allowed|denied`, and changed receipt evidence to allow only the configured Vault while denying every other challenge destination when the network is verified internal and Vault is reachable. Pass 5: added `scripts/whoathere-linux-active-probe-internal-vault-smoke.sh`, expanded receipt JSON with explicit allowed/denied destination arrays, reran focused tests and Docker smokes, and validated the public `bridge` network is rejected before Docker probe invocation. |
| Risks found | A custom Docker network could accidentally provide public egress if accepted blindly; a Vault reachability probe without network inspection could overclaim default-deny enforcement; exact allowed/denied destinations were hidden behind counts in JSON, making black-box validation too weak; the current Docker runtime still does not prove user namespace isolation, so even an otherwise complete egress receipt must not authorize execution. |
| Improvements made | The CLI now defaults to `none` but accepts `--docker-network` only after Docker reports the network is internal; non-internal networks return `linux_active_probe_docker_network_not_internal` with `docker_invoked=false`; the probe attempts TCP connectivity only to the configured Vault host provided by the CLI; receipt JSON now includes `allowed_probe_destinations` and `denied_probe_destinations`; the internal Vault smoke proves one allowed Vault destination, fourteen denied non-Vault destinations, no missing probes, and no non-Vault allows while still returning exit 20 because user namespace evidence remains unsatisfied. |
| Validation | Docker tool probe confirmed the local image includes `nc`, `wget`, BusyBox, `awk`, and `sh`; ad hoc internal-network fixture tests proved `whoathere-vault-fixture:4873` is reachable only with a Docker network alias; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli linux_active_probe_docker -- --nocapture` passed with 5 tests; `scripts/whoathere-build-linux-active-probe-image.sh` rebuilt `whoathere/linux-active-probe:local`; `scripts/whoathere-linux-active-probe-internal-vault-smoke.sh` passed with escalation for Docker access; `scripts/whoathere-linux-active-probe-docker-smoke.sh` passed; direct `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence linux-active-probe-docker --json --execute --subject launch-sha256-smoke --context-hash sha256:smoke-context --vault-host whoathere-vault-fixture:4873 --docker-network bridge` returned exit 20 with `docker_invoked=false` and `linux_active_probe_docker_network_not_internal`; full `cargo test --manifest-path whoathere/Cargo.toml` passed with 322 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; fixture, provider challenge, provider readiness, endpoint, and compatibility smokes; `bash -n scripts/*.sh`; ASCII scan; `docker ps --format ...` confirmed no active-probe containers were left behind; `docker image ls --format ...` confirmed both the BuildKit base image and local WhoaThere probe image were present. |
| Safety boundaries | Docker network mutation is limited to unique throwaway smoke networks removed on exit; the Vault fixture is an inert local container responding with empty HTTP 204 responses; the active probe attempts only the configured Vault host and does not probe public registries or DNS; custom Docker networks are refused unless Docker marks them internal; no package managers ran through protected execution; no lifecycle/build/import code executed; no public registry/index fetch occurred; no trusted proof was minted; and launch authorization did not change. |
| Open gates | Add user namespace evidence that can satisfy the receipt contract, bind successful receipts to replay-guard-owned provider challenges, define production image provenance/signing verification, and decide when an independently verified receipt can map into trusted containment and egress proofs. |
| Next owner/action | Continue with user namespace isolation evidence for the Docker/Linux active probe, then connect only replay-guard-owned satisfied receipts to proof verification while keeping launch/protect execution denied. |

## Slice: Local Linux Active Probe Image Contract

| Field | Value |
| --- | --- |
| Scope | Added a purpose-built local Docker image contract for the Linux active-probe executor so the executor no longer depends only on an anonymous inline shell probe. |
| Pass count | 5 |
| Pass artifacts/evidence | Pass 1: inspected the current Docker executor, inline probe, smoke harness, Docker image state, and build constraints. Pass 2: added `whoathere/probe-images/linux-active-probe/` with a Dockerfile and `/usr/local/bin/whoathere-active-probe` script that emits controlled `probe.*` facts, plus a constrained `.dockerignore`. Pass 3: added `scripts/whoathere-build-linux-active-probe-image.sh` to build `whoathere/linux-active-probe:local` from the already-present BuildKit base with `--pull=false`. Pass 4: changed the CLI default Docker image to the local WhoaThere probe image, made the runner prefer `/usr/local/bin/whoathere-active-probe` when present, kept inline fallback support for shell-capable images, and exposed `image_contract`/`image_contract_valid` in Docker JSON/text diagnostics. Pass 5: updated the Docker smoke to build/use the local probe image, added image-contract assertions, rebuilt the image, reran focused/full gates and all smokes, and confirmed no active-probe containers remained. |
| Risks found | A generic base image with an inline shell probe is useful for bootstrap but weak as a durable product contract; Docker build context could accidentally include unrelated workspace files without a `.dockerignore`; making the purpose-built image default means execute-mode requires the local image to be built first; image contract validity is diagnostic only and cannot be treated as proof authorization. |
| Improvements made | The preferred image is now `whoathere/linux-active-probe:local`; the image emits `probe.image_contract=whoathere-linux-active-probe.v1`; CLI output records whether that contract was present and valid; the smoke builds the image before execution; and the fallback path still works for shell-capable images while reporting `image_contract_valid=false`. |
| Validation | `scripts/whoathere-build-linux-active-probe-image.sh` built `whoathere/linux-active-probe:local` from `moby/buildkit:buildx-stable-1` with `--pull=false`; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli linux_active_probe_docker -- --nocapture`; direct `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence linux-active-probe-docker --json --execute --subject launch-sha256-smoke --context-hash sha256:smoke-context --vault-host 127.0.0.1:4873` returned expected exit 20 with `image_contract_valid=true`; `scripts/whoathere-linux-active-probe-docker-smoke.sh`; full `cargo test --manifest-path whoathere/Cargo.toml` with 321 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; fixture, provider challenge, provider readiness, endpoint, and compatibility smokes; `bash -n` over smoke scripts; ASCII scan; `docker ps --format ...` confirmed no active-probe containers were left behind; `docker image ls --format ...` confirmed both the base BuildKit image and local WhoaThere probe image were present. |
| Safety boundaries | The image build used only the local base image with `--pull=false`; the probe script emits only controlled `probe.*` facts; the Docker executor still uses no network, no-new-privileges, dropped capabilities, and resource limits; no package managers ran through protected execution; no lifecycle/build/import code executed; no public registry/index fetch occurred; no trusted proof was minted; and launch authorization did not change. |
| Open gates | Replace no-network denial with default-deny-except-configured-Vault evidence, add user namespace evidence that can satisfy the receipt contract, bind successful image-contract receipts to replay-guard-owned provider challenges, and decide how production image provenance/signing should be verified before trusting any local probe image. |
| Next owner/action | Build the next Docker active-probe iteration around a controlled Vault allow path and user namespace evidence, then map satisfied receipts into trusted proofs only behind replay-guard verification. |

## Slice: Docker Active Probe Executor Harness

| Field | Value |
| --- | --- |
| Scope | Added the first real Docker-backed Linux active-probe executor path, exposed as `whoathere evidence linux-active-probe-docker`, that consumes the challenge probe manifest, runs a hardened no-network local container only when `--execute` is explicit, emits `linux_active_probe.v1` receipt evidence, and keeps authorization/proof minting disabled. |
| Pass count | 5 |
| Pass artifacts/evidence | Pass 1: inspected provider challenge, receipt, fixture, and JSON probe-manifest contracts plus local Docker image/runtime state. Pass 2: verified the locally available `moby/buildkit:buildx-stable-1` image can run a no-network shell with overridden entrypoint and supports `unshare`, `iptables`, default seccomp, no-new-privileges, and cgroup resource limits. Pass 3: implemented `evidence linux-active-probe-docker --subject <id> --context-hash <hash> --vault-host <host> [--image <image>] [--execute] [--json]`, with non-execute fail-closed planning and execute-mode Docker invocation. Pass 4: added unit tests and `scripts/whoathere-linux-active-probe-docker-smoke.sh` to prove the executor reports real no-network Docker evidence while still failing closed. Pass 5: reviewed timeout/container cleanup risk, added Docker `--cidfile` plus best-effort `docker rm -f` cleanup on timeout, reran focused/full gates, smokes, and leftover-container checks. |
| Risks found | Docker can prove a useful local Linux no-network execution boundary, but the available image/runtime does not prove user namespace isolation and `--network none` denies the configured Vault as well as public destinations. Killing a Docker client on timeout can leave a custom container behind unless the runner tracks and cleans up the container id. A successful receipt must still not be mistaken for launch authorization or trusted proof material. |
| Improvements made | The executor now runs Docker with `--network none`, `--security-opt no-new-privileges`, `--cap-drop ALL`, `--pids-limit 64`, `--memory 128m`, `--cpus 1`, and an overridden `/bin/sh` entrypoint. It parses only controlled `probe.*` facts, emits sanitized receipt evidence, records all challenge probes as denied under no-network execution, reports Docker metadata without raw logs, times out bounded runs, and cleans up by cidfile on timeout. Current local execution exits 20 with a complete receipt shape but unsatisfied reason codes: `linux_active_probe_user_namespace_not_isolated`, `linux_active_probe_default_deny_not_attested`, `linux_active_probe_vault_probe_denied`, and `linux_active_probe_vault_probe_missing`. |
| Validation | `docker run --rm --network none --entrypoint /bin/sh moby/buildkit:buildx-stable-1 ...` capability probes; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli linux_active_probe_docker -- --nocapture`; direct `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence linux-active-probe-docker --json --execute --subject launch-sha256-smoke --context-hash sha256:smoke-context --vault-host 127.0.0.1:4873 --image moby/buildkit:buildx-stable-1` returned expected exit 20; `scripts/whoathere-linux-active-probe-docker-smoke.sh`; full `cargo test --manifest-path whoathere/Cargo.toml` with 320 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; fixture, provider challenge, provider readiness, endpoint, and compatibility smokes; `bash -n` over smoke scripts; ASCII scan; `docker ps --format ...` confirmed no active-probe containers were left behind. |
| Safety boundaries | No package manager ran through protected execution, no lifecycle/build/import code executed, no public registry/index fetch occurred, no public network probe was made, no Docker image was pulled, no host packet filter/cgroup/seccomp/namespace state was changed outside Docker's ephemeral container boundary, no raw Docker logs are rendered, no trusted provider proof was minted, and launch authorization did not change. Docker execution was explicit via `--execute` and used only the locally present BuildKit image. |
| Open gates | Add a Linux image/runtime mode that can prove user namespace isolation; add a real default-deny-except-configured-Vault network path rather than no-network denial; bind execution to replay-guard-owned challenges; map satisfied real receipts into `Verified` proofs only after independent verifier checks; decide whether Docker-on-macOS evidence is test-harness-only or acceptable for any endpoint trust boundary. |
| Next owner/action | Build the next active-probe iteration with a purpose-built local Linux probe image and a controlled Vault allow path, then keep launch authorization denied until replay-guard-owned receipt verification can produce trusted containment and egress proofs. |

## Slice: JSON Challenge Probe Manifest

| Field | Value |
| --- | --- |
| Scope | Made challenge JSON expose the exact sorted provider `probe_destinations` manifest so real active-probe executors can consume the verifier's requested destination set without reconstructing it independently. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected challenge rendering, provider diagnostics JSON, active-probe fixture JSON, and existing probe-count assertions. Pass 2: changed the shared challenge JSON renderer to include `probe_destinations` next to `probe_destination_count`. Pass 3: extended provider-readiness, explicit-challenge, and fixture CLI tests to assert public registry, configured Vault, private network, and DNS probe destinations are rendered. Pass 4: reran focused tests, a black-box JSON parse/count check, full Rust gates, provider/endpoint/compatibility smokes, and ASCII scan. |
| Risks found | A future Linux executor would otherwise need to guess or duplicate the verifier's mandatory probe set, creating drift between what the verifier required and what the executor attempted. Text output should remain compact, and the JSON manifest must not be treated as authorization or proof material. |
| Improvements made | `evidence challenge --json`, `evidence providers --json`, and `evidence linux-active-probe-fixture --json` now carry the exact challenge probe list. Existing fail-closed behavior and exit codes are unchanged. The JSON parse smoke confirms the manifest is machine-consumable and the count equals the exported list length. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_providers_json_reports_fail_closed_readiness -- --nocapture`; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_challenge_json_reports_fail_closed_attempt -- --nocapture`; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_linux_active_probe_fixture_rejects_overpermissive_profile -- --nocapture`; Python black-box parse of `evidence challenge --json` asserting `probe_destinations` contains the configured Vault, public registry, and DNS probes and that `probe_destination_count == len(probe_destinations)`; full `cargo test --manifest-path whoathere/Cargo.toml` with 317 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `scripts/whoathere-linux-active-probe-fixture-smoke.sh`; `scripts/whoathere-provider-challenge-smoke.sh`; `scripts/whoathere-provider-smoke.sh`; `scripts/whoathere-endpoint-smoke.sh`; `scripts/whoathere-compat-smoke.sh`; ASCII scan. |
| Safety boundaries | JSON contract only. No package manager ran through protected execution, no lifecycle/build/import code executed, no namespace/seccomp/cgroup/packet-filter mutation occurred, no public network probes were made, no Docker image was pulled, no trusted provider proof was minted, and launch authorization did not change. |
| Open gates | Feed this exact manifest into the real Linux active-probe executor, add replay-guard ownership around issued challenges, and verify receipt evidence against the exported probe list before mapping it into trusted proofs. |
| Next owner/action | Implement the Docker/OrbStack-backed Linux active-probe executor using `probe_destinations` as its input contract and the existing fixture validator as its expected-output contract. |

## Slice: Linux Active Probe Fixture Validator

| Field | Value |
| --- | --- |
| Scope | Added a deterministic CLI fixture validator for the Linux active-probe receipt contract so the future Docker/OrbStack Linux executor has a stable output target before receipts can influence authorization. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected Docker availability and confirmed no reusable Linux shell image was present locally; the only image starts BuildKit and was stopped cleanly. Pass 2: added `LinuxActiveProbeFixtureProfile` and `linux_active_probe_fixture_evidence` for complete, incomplete, and overpermissive receipt profiles. Pass 3: added `whoathere evidence linux-active-probe-fixture --subject <id> --context-hash <hash> --vault-host <host> --profile complete|incomplete|overpermissive [--json]`, with explicit `authorization=false`, `proof_minted=false`, and `execution_allowed=false`. Pass 4: added focused tests and `scripts/whoathere-linux-active-probe-fixture-smoke.sh`, then reran full gates and existing provider/endpoint/compatibility smokes. |
| Risks found | A real Linux executor needs a stable receipt shape before it is worth adding OS mutation; complete fixture validation could be mistaken for an authorization signal if the command did not explicitly separate receipt validity from proof minting and launch execution; relying on Docker image pulls would block in restricted/networkless runs. |
| Improvements made | The fixture command proves that a complete receipt satisfies the parser, and that incomplete/overpermissive receipts fail closed. It keeps receipt validation isolated from launch authorization, emits no trusted proofs, and makes unsafe fixture behavior visible through reason codes such as `linux_active_probe_non_vault_probe_allowed` and `linux_active_probe_denied_probe_missing`. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox linux_active_probe -- --nocapture`; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_linux_active_probe_fixture -- --nocapture`; `scripts/whoathere-linux-active-probe-fixture-smoke.sh`; full `cargo test --manifest-path whoathere/Cargo.toml` with 317 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `scripts/whoathere-provider-challenge-smoke.sh`; `scripts/whoathere-provider-smoke.sh`; `scripts/whoathere-endpoint-smoke.sh`; `scripts/whoathere-compat-smoke.sh`; shell syntax checks; ASCII scan; Python JSON parse of the complete fixture asserting receipt satisfaction while authorization and proof minting stay false. |
| Safety boundaries | Fixture and validation only. No package manager ran through protected execution, no lifecycle/build/import code executed, no namespace/seccomp/cgroup/packet-filter mutation occurred, no public network probes were made, no Docker image was pulled, no trusted provider proof was minted, and launch authorization did not change. The compatibility smoke used only generated inert local fixtures with npm scripts disabled and pip dependency resolution disabled. |
| Open gates | Build the actual Docker/OrbStack-backed Linux active-probe executor against this fixture contract, then add replay-guard ownership and only map a satisfied real receipt into trusted proofs after independent verification. |
| Next owner/action | Create or locate a reusable Linux test image and implement a no-public-network active probe runner that emits `linux_active_probe.v1` evidence matching this fixture contract. |

## Slice: Linux Active Probe Receipt Contract

| Field | Value |
| --- | --- |
| Scope | Added a Linux active-probe receipt model that defines the challenge-bound evidence a real Linux provider must produce before containment and Vault-only egress proofs can ever become trusted. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the `ProofProvider`, provider challenge, readiness, and `launch provider-check` contracts. Pass 2: added `LinuxActiveProbeReceipt` parsing for `linux_active_probe.v1` evidence, including challenge id, subject, context hash, Vault host, namespace, `no_new_privs`, seccomp, cgroup, default-deny egress, mutation-scope, and probe verdict fields. Pass 3: added positive and negative sandbox tests for satisfying receipts, mutated context, over-permissive non-Vault egress, missing denied probes, and public-probe attempts. Pass 4: rendered the Linux active-probe receipt in text/JSON provider diagnostics, reran focused/full gates, and smoke-tested provider/endpoint/compatibility paths. |
| Risks found | Read-only readiness had no structured place to distinguish passive host capability probes from future active proof receipts; a future Linux provider could otherwise overclaim enforcement without binding the receipt to the launch challenge, proving every mandatory denied destination, or showing mutations stayed inside a provider-owned namespace. |
| Improvements made | Linux diagnostics now expose `provider_active_probe provider=linux ... satisfied=false ... reason_codes=[...]` and JSON `active_probe_receipt`. The receipt contract rejects missing or wrong schema, incomplete status, off-host evidence, challenge/subject/context/Vault mismatches, missing namespace/seccomp/cgroup/default-deny attestations, package execution, public-network probes, invalid mutation scope, missing Vault allowance, missing denied probes, and non-Vault allowed probes. Current providers still emit read-only evidence only, so the receipt remains unsatisfied and fail-closed. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox linux_active_probe -- --nocapture`; focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli evidence_providers -- --nocapture`; full `cargo test --manifest-path whoathere/Cargo.toml` with 313 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `scripts/whoathere-provider-challenge-smoke.sh`; `scripts/whoathere-provider-smoke.sh`; `scripts/whoathere-endpoint-smoke.sh`; `scripts/whoathere-compat-smoke.sh`; shell syntax checks; ASCII scan; direct `evidence providers --scope linux` smoke confirming the active-probe receipt is rendered and unsatisfied; Python JSON parse of `evidence providers --json --scope linux` asserting `active_probe_receipt.satisfied == false`. |
| Safety boundaries | Contract and diagnostics only. No package manager ran through WhoaThere execution, no lifecycle/build/import code executed, no namespace/seccomp/cgroup/packet-filter mutation occurred, no public network probes were made, no real provider proofs were minted, and launch authorization did not change. The compatibility smoke used only generated inert local fixtures with npm scripts disabled and pip dependency resolution disabled. |
| Open gates | Implement a Linux provider collector that emits this receipt from a controlled namespace/egress harness, add replay-guard ownership around issued challenges, and only then consider mapping a satisfied receipt into trusted `Verified` proofs. |
| Next owner/action | Build the Linux active probe executor in a Docker/OrbStack-backed fixture first, then port the same receipt contract to a local Linux endpoint runner. |

## Slice: Launch Provider Check Diagnostic

| Field | Value |
| --- | --- |
| Scope | Added `whoathere launch provider-check` as a one-shot diagnostic that builds the launch plan and evaluates its generated proof challenge against the current local provider skeleton without enabling execution. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected launch planning, provider challenge rendering, and explicit provider challenge evaluation. Pass 2: added parser and help support for `launch provider-check [--workspace ...] [--vault-origin ...] [--runtime-dir ...] [--containment-available] [--egress-enforced] <tool> -- <args>`. Pass 3: rendered the normal launch-plan output plus `launch_provider_check_*` status, provider, challenge, probe, proof, reason, and exit-code diagnostics. Pass 4: added a focused CLI test, smoke-ran the command, reran full Rust gates, and reran provider/endpoint/compatibility smokes. |
| Risks found | Copy/paste provider challenge commands are useful but too manual for repeated launch diagnostics; launch planning needed an internal provider-evaluation bridge that still does not execute package managers; provider diagnostics must remain evidence, not authorization, until real provider-owned proof verification exists. |
| Improvements made | `launch provider-check` now evaluates the exact launch-generated challenge through the current provider skeleton, appends machine-readable `launch_provider_check_*` lines, reports fail-closed unsatisfied proof reasons, and leaves the launch decision denied. |
| Validation | Focused `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli launch_provider_check -- --nocapture`; smoke-ran `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- launch provider-check --execute --workspace whoathere/tests/fixtures/source-clean --vault-origin http://127.0.0.1:4873 --egress-enforced --containment-available npm -- ci` and confirmed expected exit 20, `launch_plan_status=blocked`, `runtime_materialized=false`, and `launch_provider_check_status=fail_closed`; full `cargo test --manifest-path whoathere/Cargo.toml` with 311 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `scripts/whoathere-provider-challenge-smoke.sh`; `scripts/whoathere-provider-smoke.sh`; `scripts/whoathere-endpoint-smoke.sh`; `scripts/whoathere-compat-smoke.sh`; shell syntax checks. |
| Safety boundaries | Diagnostic-only bridge. It does not run npm or pip, create VMs or namespaces, mutate OS/network state, run probes, fetch registries, materialize runtimes, mint verified proofs, or change `execution_allowed=false`. |
| Open gates | Replace skeleton provider attempts with real macOS/Linux provider collectors and define replay-guard ownership before any provider-check result can influence launch authorization. |
| Next owner/action | Implement the first real current-host provider collector behind this diagnostic path, starting with same-subject containment and default-deny Vault-only egress proof collection in controlled fixtures. |

## Slice: Launch-To-Provider Challenge Bridge

| Field | Value |
| --- | --- |
| Scope | Made `launch plan` expose the exact provider challenge command for the launch subject/context/Vault host it generated. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected launch proof challenge rendering and the new explicit `evidence challenge` command. Pass 2: added `proof_challenge_context_hash` to launch output so the full provider challenge tuple is visible. Pass 3: added `provider_challenge_command=whoathere evidence challenge --scope current ...` with shell-safe quoting and a `none` value when no proof challenge exists. Pass 4: updated launch tests, smoke-ran `launch plan`, reran full gates, and reran provider/endpoint/compatibility smokes. |
| Risks found | Launch plans previously emitted challenge subject and Vault host but not a direct command to evaluate that exact challenge; operators could accidentally copy the wrong context hash into provider diagnostics; future provider integration needs a low-friction way to reproduce launch proof failure outside the launch planner. |
| Improvements made | Blocked launch plans now expose the context hash and copy-pasteable provider challenge command, tying launch planning to provider diagnostics without invoking providers, enabling proofs, or changing execution decisions. |
| Validation | Focused launch CLI test for `provider_challenge_command`; smoke-ran `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- launch plan --execute --workspace whoathere/tests/fixtures/source-clean --vault-origin http://127.0.0.1:4873 --egress-enforced --containment-available npm -- ci` and confirmed expected exit 20 plus `provider_challenge_command`; full `cargo test --manifest-path whoathere/Cargo.toml` with 310 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `scripts/whoathere-provider-challenge-smoke.sh`; `scripts/whoathere-provider-smoke.sh`; `scripts/whoathere-endpoint-smoke.sh`; `scripts/whoathere-compat-smoke.sh`. |
| Safety boundaries | Rendering-only bridge. It does not execute the provider command, run package managers, create VMs or namespaces, mutate OS/network state, fetch registries, or mint verified proofs. Launch remains blocked with `execution_allowed=false`. |
| Open gates | Replace copy/paste diagnostics with an internal provider-evaluation path only after real providers can satisfy the explicit challenge contract and replay-guard ownership is defined. |
| Next owner/action | Use the emitted command while developing the first real macOS/Linux provider to compare launch-generated challenges against provider attempts. |

## Slice: Explicit Provider Challenge Evaluation Contract

| Field | Value |
| --- | --- |
| Scope | Added `whoathere evidence challenge` and `scripts/whoathere-provider-challenge-smoke.sh` so callers can evaluate a specific launch-subject challenge against one local provider without enabling execution. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected CLI evidence-provider parsing/rendering and the sandbox `ProviderVerificationChallenge`/`ProofProvider::evaluate_challenge` contract. Pass 2: added `evidence challenge --subject <id> --context-hash <hash> --vault-host <host> [--json] [--scope current|linux|macos]`, defaulting to current provider and rejecting missing fields or multi-provider scope as misuse. Pass 3: rendered text/JSON challenge, challenge-attempt, containment, and egress summaries with `exit_code=20` until the provider satisfies the challenge. Pass 4: added focused CLI tests, real command smokes, a dedicated provider challenge smoke script, full workspace gates, and compatibility/endpoint/provider smoke regressions. |
| Risks found | Provider diagnostics used built-in synthetic challenges only; there was no CLI contract for a future launch planner or CI job to ask a provider to evaluate a concrete subject/context/vault challenge; allowing implicit defaults for challenge inputs would make provider proofs easier to misuse. |
| Improvements made | Explicit challenge evaluation now requires caller-supplied subject, context hash, and Vault host. It selects exactly one provider, emits the challenge and attempt contract in text or JSON, returns misuse for malformed input, and returns fail-closed deny while the local provider skeleton cannot produce verified containment and Vault-only egress proofs. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli` with 75 CLI tests; text and JSON `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence challenge ...` smokes expected exit 20; `scripts/whoathere-provider-challenge-smoke.sh`; full `cargo test --manifest-path whoathere/Cargo.toml` with 310 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `scripts/whoathere-provider-smoke.sh`; `scripts/whoathere-endpoint-smoke.sh`; `scripts/whoathere-compat-smoke.sh`; shell syntax checks. |
| Safety boundaries | Diagnostic-only command. It does not execute package managers, create VMs or namespaces, install seccomp/Landlock/cgroup rules, alter packet filters, run network probes, fetch registries, mutate OS state, or mint verified proofs. |
| Open gates | Wire launch planning to request provider challenge attempts once real providers exist, add replay-guard ownership around issued challenges, and keep execution denied until provider attempts satisfy every launch proof gate. |
| Next owner/action | Implement a real macOS or Linux provider that can satisfy this explicit challenge contract in a controlled test fixture before enabling any package-manager execution. |

## Slice: Provider Readiness Hardening And Smoke Harness

| Field | Value |
| --- | --- |
| Scope | Tightened local provider readiness diagnostics and added `scripts/whoathere-provider-smoke.sh` as a current-host fail-closed provider regression gate. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: reviewed the launch proof contract and confirmed high-risk execution still requires verified containment and verified Vault-only egress proofs that share subject, provider session, launch context, challenge, and freshness. Pass 2: made Linux and macOS readiness require `provider_target_matches_host=true` before prerequisites can be considered present. Pass 3: added read-only Linux evidence for namespace baselines and common namespace/packet-filter tools, and made Linux prerequisites require non-executed namespace creation plus packet-filter tooling signals. Pass 4: added negative tests and a provider smoke harness that asserts current-host readiness remains fail-closed with no package execution, OS mutation, network mutation, or public probing. |
| Risks found | Linux readiness previously treated readable namespace links as enough to say namespace primitives were observed; off-host provider evidence could look prerequisite-complete in synthetic tests; provider diagnostics were mostly exercised indirectly through endpoint setup; future work needs a small gate proving provider probes stay non-mutating while fail-closed. |
| Improvements made | Provider readiness now records `target_matches_host`, Linux namespace/packet-filter tool availability, and target-host mismatch reason codes. Linux `required_primitives_present` now requires current-host evidence plus namespace creation and packet-filter tooling signals. macOS `required_primitives_present` now also requires current-host evidence. The provider smoke verifies `evidence providers --json --require-ready --scope current` exits 20 and reports safe read-only posture. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-cli` with 36 sandbox tests and 72 CLI tests; full `cargo test --manifest-path whoathere/Cargo.toml` with 307 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `scripts/whoathere-provider-smoke.sh`; `scripts/whoathere-endpoint-smoke.sh`; `scripts/whoathere-compat-smoke.sh`; shell syntax checks. |
| Safety boundaries | Diagnostics and smoke tests only. No provider proof verification was enabled, no high-risk package execution was allowed, no npm lifecycle or pip build/import path ran, no OS sandbox or packet-filter mutation occurred, and no public network probe was attempted. |
| Open gates | Implement a real provider-owned verification flow that can create a launch subject, prove VM/namespace containment, prove default-deny Vault-only egress, bind both proofs to the launch context and challenge, and then still keep actual package-manager execution gated until end-to-end contract tests pass. |
| Next owner/action | Start the first real provider spike: macOS VM/packet-filter proof collector or Linux namespace/seccomp/cgroup/packet-filter proof collector, with this smoke harness guarding the diagnostic path. |

## Slice: Endpoint Interception Smoke Harness

| Field | Value |
| --- | --- |
| Scope | Added `scripts/whoathere-endpoint-smoke.sh` as a repeatable local regression that chains endpoint setup diagnostics, shim materialization, PATH resolution, and expected-denied npm/pip interception. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: reviewed the existing endpoint setup, shim install, env-default, and fail-closed launch-gate behavior. Pass 2: added a temp-workspace smoke harness with command timeouts, setup diagnostics, shim materialization checks, PATH resolution assertions, and expected-denied `npm ci` plus `pip install fixture` runs through generated shims. Pass 3: reduced successful output to decision/gate lines while retaining full captured logs on assertion failure. Pass 4: ran focused endpoint smoke, full Rust gates, compatibility smoke, shell syntax checks, and ASCII scan. |
| Risks found | Endpoint setup and shim install could each pass while the combined developer workflow still failed; PATH shims could fail to shadow real package-manager binaries; a smoke harness that prints full cargo/package output is too noisy for CI and resumable overnight work; expected-denied interception must prove denial happens before real package-manager execution. |
| Improvements made | The harness now creates isolated temp workspace/shim dirs, verifies `endpoint setup` exits fail-closed with current-provider readiness instructions, verifies generated shims call `protect --execute`, asserts `command -v npm` and `command -v pip` resolve to the temp shims, and confirms both npm and pip workflows deny at the launch proof gate with `execution_reason=launch_plan_blocked`. |
| Validation | `bash -n scripts/whoathere-endpoint-smoke.sh`; `scripts/whoathere-endpoint-smoke.sh`; full `cargo test --manifest-path whoathere/Cargo.toml` with 305 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `scripts/whoathere-compat-smoke.sh`; `bash -n scripts/whoathere-compat-smoke.sh`; ASCII scan. |
| Safety boundaries | Temp directories only. No shell profile edits, no durable PATH mutation, no npm lifecycle execution, no pip build/import execution, no public registry/index fetch, no OS sandbox mutation, and no production provider proof minting. Intercepted package-manager commands were denied before npm or pip themselves executed. |
| Open gates | Promote this harness to the CI integration gate once environment prerequisites are formalized; extend it after real provider verification exists to prove allow-path execution only inside verified containment and Vault-only egress. |
| Next owner/action | Replace fail-closed provider skeletons with the first real macOS/Linux provider proof collector, keeping this smoke as the endpoint wiring regression. |

## Slice: Endpoint Setup Diagnostics

| Field | Value |
| --- | --- |
| Scope | Added `whoathere endpoint setup` as a diagnostic setup/verification command for local endpoint interception prerequisites. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: reviewed the endpoint shim/env/provider-readiness open gate and current CLI command shape. Pass 2: added parser support for `endpoint setup` with flags and env defaults for shim dir, workspace, Vault origin, policy, and audit path. Pass 3: implemented diagnostic rendering of shim install command, shell exports, current provider platform, provider readiness command, and fail-closed readiness state without mutation. Pass 4: added focused CLI tests, ran an endpoint setup smoke with temp env defaults, full Rust gates, compatibility smoke, and ASCII scan. |
| Risks found | Developers need a single setup check before putting shims on PATH; scattering shim install, env exports, and provider readiness across separate commands is error-prone; setup tooling must not mutate shell profiles or imply providers are ready while proof verification is unimplemented. |
| Improvements made | `endpoint setup` now emits the exact `shim install` and export commands, checks required values, reports path existence, uses `evidence providers --json --require-ready --scope current` as the readiness contract, exits 20 while current-provider verification is unavailable, and exits 64 for missing required setup inputs. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli` with 72 CLI tests; endpoint setup smoke with temp `WHOATHERE_SHIM_DIR`, `WHOATHERE_WORKSPACE`, and `WHOATHERE_VAULT_ORIGIN` expected-denied with exit 20; full `cargo test --manifest-path whoathere/Cargo.toml` with 305 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `scripts/whoathere-compat-smoke.sh`; ASCII scan. |
| Safety boundaries | Diagnostic-only command. No shell profile edits, no shim file creation, no PATH mutation, no package-manager execution, no public registry/index fetch, no OS sandbox mutation, and no production provider proof minting. |
| Open gates | Once real provider verification exists, decide whether `endpoint setup` should optionally materialize shims or stay pure-plan; add shell-specific output variants if needed for fish/PowerShell later. |
| Next owner/action | Implement the first real current-host proof provider or add an endpoint setup smoke that chains `endpoint setup`, `shim install`, and expected-denied PATH interception in one command. |

## Slice: Endpoint Shim Execution Defaults

| Field | Value |
| --- | --- |
| Scope | Made materialized PATH shims behave like intercepted execution attempts and added safe environment defaults for workspace, Vault origin, policy, and audit paths. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected shim materialization, `protect` parsing, runner behavior, and existing shim tests. Pass 2: added `WHOATHERE_WORKSPACE`, `WHOATHERE_VAULT_ORIGIN`, `WHOATHERE_POLICY`, and `WHOATHERE_AUDIT_PATH` defaults with explicit CLI flags taking precedence and no env knob for enabling execution. Pass 3: changed generated shims to call `whoathere protect --execute <tool> -- "$@"`, updated manifest output/tests, and verified relative shim targets still cannot run readonly probes directly. Pass 4: ran focused CLI tests, real PATH shim smoke, full Rust gates, compatibility smoke, and ASCII scan. |
| Risks found | Preview-only shims skipped the launch proof gate and made real interception look less like an attempted install; hardcoding workspace/Vault/policy/audit paths into shim files would make shims brittle; env defaults could accidentally weaken controls if they enabled execution or overrode explicit flags. |
| Improvements made | Shims now request execution so high-risk package-manager calls pass through package identity, source scan, launch-context, and proof gates before being denied. `protect` fills missing workspace/Vault/policy/audit values from env defaults while keeping explicit CLI flags authoritative and keeping execution controlled only by `--execute`/shim target. Dry-run output documents the supported env defaults. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli` with 69 CLI tests; real PATH shim smoke using temp shims/workspace and `npm ci` expected-denied with exit 20 at `launch_plan_status=blocked`; full `cargo test --manifest-path whoathere/Cargo.toml` with 302 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `scripts/whoathere-compat-smoke.sh`; ASCII scan. |
| Safety boundaries | No npm lifecycle execution, no pip build/import execution, no protected install execution, no public registry/index fetch, no OS sandbox mutation, and no production provider proof minting. The PATH smoke used a temp shim dir and temp workspace; the intercepted `npm ci` was denied before npm itself executed. |
| Open gates | Add a setup/doctor workflow that validates env defaults and provider readiness before users place shims on PATH; decide how to discover real package-manager binaries for safe readonly probes without PATH recursion. |
| Next owner/action | Build a documented endpoint setup/verification command around shim install, env defaults, and `evidence providers --scope current --require-ready`. |

## Slice: Compatibility Smoke Harness Reliability

| Field | Value |
| --- | --- |
| Scope | Hardened the local npm/pip compatibility smoke harness and stabilized the bounded local-dev Vault server read path after a transient loopback startup/read failure. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: observed and investigated a transient npm `ECONNREFUSED`/timeout in `scripts/whoathere-compat-smoke.sh`; retained logs showed `dev_server_read_failed` with zero served requests. Pass 2: added portable Python-backed command timeouts, server readiness death checks, bounded server-exit waits, and log printing for startup/package-manager/route/server failures. Pass 3: fixed the local-dev server to set accepted streams back to blocking mode with a bounded read timeout before reading HTTP requests. Pass 4: reran focused Vault dev tests, shell syntax check, hardened smoke harness, full Rust gates, Clippy, and ASCII scan. |
| Risks found | The harness could wait too long inside npm/pip retries; server logs were not printed for package-manager failures; Bash `set -e` is suppressed inside functions called from `||`, so metadata checks could run after npm timeout; accepted streams from a nonblocking listener could fail immediate reads on macOS and exit the server before package-manager requests. |
| Improvements made | Package-manager and metadata-check subprocesses now run with explicit timeouts and immediate `return 1` on failure. Server startup detects early process death and prints logs. Server exit waits are bounded and route checks print logs on missing patterns. The dev server normalizes accepted streams to blocking mode and applies a read timeout before parsing requests. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev` with 58 Vault dev tests; `bash -n scripts/whoathere-compat-smoke.sh`; `scripts/whoathere-compat-smoke.sh`; full `cargo test --manifest-path whoathere/Cargo.toml` with 300 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; ASCII scan. |
| Safety boundaries | Local-dev reliability only. No public registry/index fetch, no npm lifecycle execution, no pip dependency resolution, no Python import-time execution, no protected product install execution, no non-loopback binding, and no production auth/TLS claim. |
| Open gates | Promote the smoke harness to a CI-owned integration target or `cargo xtask`, define environment prerequisites, and decide whether transient loopback failures should retry once or remain strict fail-fast in CI. |
| Next owner/action | Use the hardened smoke harness as the regression gate for future local-dev registry compatibility changes. |

## Slice: Scoped Provider Readiness Diagnostics

| Field | Value |
| --- | --- |
| Scope | Added explicit provider diagnostic scoping so readiness gates can evaluate all providers, the current host provider, or a named target provider without weakening fail-closed behavior. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected `evidence providers` parsing, rendering, readiness calculation, help text, and existing fail-closed tests. Pass 2: added `ProviderScope` with default `all`, parser support for `--scope all|current|linux|macos`, invalid-scope misuse handling, and current-host platform detection. Pass 3: filtered text/JSON provider diagnostics before readiness calculation and rendered top-level `provider_scope` plus `current_provider_platform`. Pass 4: added focused CLI tests, scoped command smokes, full Rust gates, and compatibility smoke regression. |
| Risks found | A single endpoint readiness gate that always required both Linux and macOS providers would remain false even after one platform's real provider was implemented; changing the default scope could accidentally weaken existing fleet-wide expectations; invalid scope values needed an explicit deny/misuse path rather than silent fallback. |
| Improvements made | `evidence providers` now accepts `--scope all|current|linux|macos`; default `all` preserves existing conservative behavior. `--scope current` filters diagnostics and `provider_ready` to the current endpoint provider. Invalid scopes return `status=error`, `reason_code=invalid_provider_scope`, and exit 64. `--require-ready` remains fail-closed for every scope until scoped providers can actually verify enforcement. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli` with 67 CLI tests; full `cargo test --manifest-path whoathere/Cargo.toml` with 300 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; scoped command smokes for `--json --require-ready --scope current` expected exit 20, `--scope linux` filtered text diagnostics, and `--json --scope windows` expected exit 64; `scripts/whoathere-compat-smoke.sh` passed on rerun. |
| Safety boundaries | Diagnostics only. No package-manager install execution, lifecycle scripts, Python build/import execution, public registry fetches, OS namespace/VM creation, packet-filter changes, network probes, or production provider proof minting. |
| Open gates | Decide CI defaults once real providers exist: `--scope current` is likely right for endpoint admission, while `--scope all` remains useful for fleet conformance and release checks. |
| Next owner/action | Use scoped readiness in the first real Linux/macOS provider integration tests so one platform can become ready without requiring the other platform to be complete. |

## Slice: Local Provider Posture Evidence

| Field | Value |
| --- | --- |
| Scope | Added explicit, structured posture evidence for fail-closed Linux/macOS local proof providers so diagnostics distinguish read-only readiness probes from active containment or egress verification. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected sandbox provider contracts, readiness parsers, challenge-first proof attempts, and CLI text/JSON diagnostics. Pass 2: added schema-versioned provider posture evidence for host platform, target platform, target/host match, active verification, package execution, OS mutation, network mutation, and public network probe state. Pass 3: exposed parsed posture in `evidence providers` text output and JSON provider objects while preserving raw redacted evidence arrays. Pass 4: ran focused sandbox/CLI tests and the fail-closed `evidence providers --json --require-ready` diagnostic smoke. |
| Risks found | Read-only provider probes could be mistaken for real enforcement; cross-platform diagnostics did not make host-versus-target applicability obvious; future automation needed stable posture fields instead of scraping raw evidence strings; diagnostics must not claim that active verification, package execution, OS mutation, network mutation, or public network probing occurred. |
| Improvements made | Local providers now emit `provider_evidence_schema=local_provider_readiness.v1`, `provider_host_platform`, `provider_target_platform`, `provider_target_matches_host`, and explicit false active/no-mutation/no-execution flags. The sandbox crate parses those into `LocalProviderEvidencePosture`; the CLI renders `provider_posture` text lines and JSON `posture` objects beside readiness and verification plans. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-cli` with 34 sandbox tests and 64 CLI tests; full `cargo test --manifest-path whoathere/Cargo.toml` with 297 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; ASCII scan; `scripts/whoathere-compat-smoke.sh`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers --json --require-ready` expected-denied with exit 20, `status=fail_closed`, `provider_ready=false`, host/target posture, and explicit no-active/no-mutation/no-public-probe flags. |
| Safety boundaries | Diagnostics and tests only. No npm/pip install execution, lifecycle scripts, Python build/import execution, public registry fetches, OS namespace/VM creation, seccomp/Landlock/cgroup changes, packet-filter changes, active network probing, or production provider proof minting. |
| Open gates | Replace read-only posture with real active collector evidence only after provider-owned verification can prove same-subject containment and default-deny Vault-only egress; add durable audit/CI contract tests for posture fields when real providers land. |
| Next owner/action | Implement the first real Linux or macOS provider collector/verifier behind this posture contract, keeping `proof_verification_enabled=false` until all required checks are independently verifiable. |

## Slice: Repeatable Local Compatibility Smoke Harness

| Field | Value |
| --- | --- |
| Scope | Added `scripts/whoathere-compat-smoke.sh` to rerun the npm and pip local-dev Vault compatibility proof without reconstructing manual commands. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: converted the manual dynamic-port npm/pip sequence into a bounded local script. Pass 2: added allocated loopback ports, temp npm/pip work/cache directories, and safe package-manager flags. Pass 3: verified server summaries include expected sanitized route logs and no body capture. Pass 4: ran the script, shell syntax check, and full Rust validation gates. |
| Risks found | Manual smoke steps are easy to run with the wrong port or unsafe flags; fixed temp dirs can pollute local state; servers must not remain running after failures; package-manager output alone does not prove server audit behavior. |
| Improvements made | The script picks loopback ports, starts bounded dev servers, uses a health check, runs npm with lifecycle/audit/fund disabled, runs pip with dependency resolution disabled, checks installed fixture metadata without Python import execution, waits for server exit, verifies request count and sanitized route kinds, and cleans temp state unless `WHOATHERE_KEEP_SMOKE_TEMP=1` is set. |
| Validation | `scripts/whoathere-compat-smoke.sh` passed; `bash -n scripts/whoathere-compat-smoke.sh` passed; full `cargo test --manifest-path whoathere/Cargo.toml` with 297 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; ASCII scan; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`. |
| Safety boundaries | Local-dev smoke only; no public registry/index fetch, no npm lifecycle execution, no pip dependency resolution, no Python import-time execution, no protected product install execution, and no production auth/TLS claim. |
| Open gates | Decide whether to promote this to `cargo xtask` or ignored CI integration test; add timeout hardening around package-manager subprocesses if it becomes CI-owned; keep real protected installs gated on containment and egress verification. |
| Next owner/action | Begin real Linux/macOS proof-provider evidence collector work, or formalize this script as the integration gate used by future Vault registry changes. |

## Slice: Host-Aware Local-Dev Registry Compatibility

| Field | Value |
| --- | --- |
| Scope | Removed the hardcoded `127.0.0.1:4873` assumption from package-manager compatibility metadata by deriving absolute artifact URLs from validated loopback Host headers. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: identified fixed-port tarball/wheel URLs as a blocker for repeatable integration smokes. Pass 2: made npm and PyPI compat metadata request-aware while preserving the socketless default. Pass 3: added loopback Host validation and fail-closed public Host rejection tests. Pass 4: reran focused tests and real npm/pip smokes on non-default loopback ports with two-request bounded servers. |
| Risks found | Reflecting arbitrary Host headers could turn local metadata into public or credentialed URLs; keeping a fixed port made integration tests fragile and blocked allocated-port harnesses; package managers require absolute URLs that match the server they contacted. |
| Improvements made | Compat metadata now accepts only explicit loopback Host authorities (`127.0.0.1:<port>`, `localhost:<port>`, `[::1]:<port>`), rejects public/malformed Host values with `registry_compat_host_not_loopback`, and emits tarball/wheel URLs on the active loopback port. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli` with 64 CLI tests and 58 Vault dev tests; temp-dir npm install against `127.0.0.1:48743` with scripts/audit/fund disabled; temp-dir pip install against `127.0.0.1:48742` with dependency resolution disabled; both bounded servers exited after two sanitized requests; full `cargo test --manifest-path whoathere/Cargo.toml` with 297 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; ASCII scan; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`. |
| Safety boundaries | Host-aware metadata is local-dev only; no public registry/index fetch, no npm lifecycle execution, no pip dependency resolution, no Python import-time execution, no protected product install execution, and no production auth/TLS claim. |
| Open gates | Bind future production metadata to authenticated tenant origins rather than Host reflection alone. |
| Next owner/action | Repeatable harness completed by the slice above; continue with real Linux/macOS proof-provider evidence collectors. |

## Slice: Safe Local-Dev Package Archives And Package-Manager Smokes

| Field | Value |
| --- | --- |
| Scope | Replaced placeholder package-manager compatibility bytes with deterministic safe npm `.tgz` and PyPI wheel fixtures, then proved npm and pip can install those fixtures from a bounded loopback local-dev Vault server. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected existing 25-byte promoted-byte path and kept older simulation fixtures intact for regression coverage. Pass 2: added offline deterministic archive builders and content-addressed compat generations for npm and PyPI. Pass 3: updated compat metadata to use `.tgz`, SRI `sha256-...`, `.whl`, and standard `#sha256=` fragments. Pass 4: ran unit tests, socketless `vault dev-http` smokes, real temp-dir npm/pip smokes against bounded loopback Vault, and Rust validation gates. |
| Risks found | Placeholder bytes could not prove real npm/pip compatibility; PyPI Simple links without wheel filenames and `#sha256=` fragments were weak; npm integrity needed package-manager-shaped SRI rather than internal `sha256:<hex>` metadata; adding real package-manager smokes could accidentally run scripts, resolve dependencies, or touch public registries. |
| Improvements made | Compat npm metadata now points to a generated `.tgz` with empty scripts and SRI integrity; compat PyPI metadata now points to a generated wheel with a standard hash fragment; both archives are generated locally, hashed with the in-tree SHA-256 implementation, served only through exact promoted-cache lookup, and validated by real npm/pip temp-dir smokes with npm scripts disabled and pip dependency resolution disabled. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli` with 64 CLI tests and 55 Vault dev tests; socketless smokes for npm packument, npm archive Range, PyPI Simple, and PyPI wheel HEAD; bounded loopback `npm install fixture@1.0.0 --ignore-scripts --no-audit --no-fund` from a fresh `/private/tmp` project; bounded loopback `python3 -m pip install --no-deps --disable-pip-version-check --target <temp-target> fixture==1.0.0`; full `cargo test --manifest-path whoathere/Cargo.toml` with 294 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`. |
| Safety boundaries | No real malware, no public registry/index fetch, no npm lifecycle execution, no pip dependency resolution, no Python import-time execution, no protected product install execution, no durable object writes, and no production auth/TLS claim. Package-manager smokes used generated safe local fixtures in `/private/tmp` only. |
| Open gates | Move smokes into a repeatable integration harness; add production authenticated registry endpoints and durable promoted-object storage; keep protected install execution gated until real containment and egress proof providers verify enforcement. |
| Next owner/action | Port-allocation blocker completed by the Host-aware compatibility slice above; next build a reusable integration-smoke command or test harness. |

## Slice: Package-Manager-Shaped Local-Dev Registry Compatibility

| Field | Value |
| --- | --- |
| Scope | Added explicit local-dev npm/PyPI compatibility aliases under `/v1/registry-compat` for future package-manager client testing, while preserving fixed-fixture, promoted-only, no-fetch semantics. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: added npm packument and PyPI Simple compatibility routes that do not require query flags. Pass 2: reused exact promoted artifact/cache-key byte serving for compat tarball/file aliases, including Range and HEAD behavior. Pass 3: added sanitized request-log route classification for compat routes. Pass 4: ran focused tests and socketless CLI smokes for npm metadata, npm range tarball, PyPI Simple metadata, and PyPI file HEAD. |
| Risks found | Compatibility-looking paths can be mistaken for a real registry; package-byte aliases could accidentally bypass exact promoted-cache lookup; new route families could fall out of sanitized audit classification. |
| Improvements made | Compat routes are fixed to the inert promoted fixture and fail closed for unknown packages; compat byte aliases delegate to the same promoted-cache lookup, validator, conditional, HEAD, and Range path as simulation routes; request logs classify compat paths into npm/PyPI metadata and byte categories without logging bodies or secrets. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli` with 64 CLI tests and 53 Vault dev tests; socketless `vault dev-http` smokes for `/v1/registry-compat/npm/fixture`, npm tarball Range, `/v1/registry-compat/pypi/simple/fixture/`, and PyPI file HEAD; full `cargo test --manifest-path whoathere/Cargo.toml` with 292 unit tests plus doctests; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; ASCII scan; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`. |
| Safety boundaries | Local-dev only; no npm/pip execution, lifecycle scripts, Python build/import execution, public registry fetches, production auth/TLS claim, durable object storage, or real package payloads. |
| Open gates | Add authenticated tenant-aware registry endpoints, durable promoted-object storage, real package-manager client compatibility fixtures, and production request/audit logging after containment and egress providers are verified. |
| Next owner/action | Completed by the safe local-dev package archive slice above; next durable-data-plane work still needs authenticated registry endpoints and persistent promoted object storage. |

## Slice: Policy Loading And Safe Wrapper Execution

| Field | Value |
| --- | --- |
| Scope | Local policy file parsing, schema-version rejection, `protect --policy`, process exit enforcement, guarded readonly execution planning, safer shim materialization. |
| Pass count | 5 |
| Pass artifacts/evidence | Source review from wrapper reviewer, implementation tests, format/test/Clippy gates, CLI smoke checks, report audit. |
| Risks found | Deny decisions only printed; generated shims trusted PATH and had weak shell quoting; invalid policy was not in wrapper path; schema versions accepted blindly; command classification alone is insufficient for install execution. |
| Improvements made | `main` exits with inferred command result code; shims use current executable path, single-quote shell quoting, and create-new writes; `protect --policy` denies invalid policy before execution; unknown schema versions are rejected; readonly version probes are explicitly low-risk; installs remain gated. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml`; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; CLI smoke commands. |
| Safety boundaries | No package install execution; readonly execution is explicit opt-in only; install/build/import commands remain refused. |
| Open gates | Real package-manager install wrapper requires sanitized env/config, Vault-only registry/index rewriting, lockfile/requirements source scanning, and OS containment. |
| Next owner/action | Implement source-file scanners for `.npmrc`, pip config, lockfiles, and requirements before allowing package installs. |

## Slice: Loopback Dev Vault Surface

| Field | Value |
| --- | --- |
| Scope | Local-dev in-memory HTTP router for health, readiness, evidence profile listing, and admission simulation. |
| Pass count | 4 |
| Pass artifacts/evidence | Dev Vault reviewer findings, implementation tests, no-registry-url tests, loopback validation tests. |
| Risks found | Generic Vault egress allowed arbitrary loopback; admission decision path panicked on unknown request ID; registry-compatible serving must stay gated; socket bind tests can be environment-fragile. |
| Improvements made | Added `whoathere-vault-dev` pure router; strict `SocketAddr` loopback validation rejects hostnames and wildcard binds; unknown admission requests return fail-closed errors; dev HTTP exposes no registry/package serving; default tests avoid actual socket bind. |
| Validation | `whoathere-vault-dev` unit tests for `/healthz`, `/readyz`, `/v1/evidence-profiles`, `/v1/admission-simulations`, unknown routes, and loopback validation. |
| Safety boundaries | In-memory only, unauthenticated local-dev simulation, no TLS claim, no persistence, no registry compatibility, no upstream fetch. |
| Open gates | Real Vault service needs auth, TLS, persistent DB/storage, queues/workflows, OpenAPI, IaC, performance tests, and production audit controls. |
| Next owner/action | Add a real loopback server wrapper only after deciding lifecycle, auth posture, and test strategy for socket permissions. |

## Slice: Reporting And Quality Handoff

| Field | Value |
| --- | --- |
| Scope | Keep morning report current, remove stale pass-count ambiguity, and record what is built versus gated. |
| Pass count | 3 |
| Pass artifacts/evidence | Handoff reviewer findings, this ledger, morning report update, final verification commands. |
| Risks found | Earlier docs had multiple pass-count summaries; some validation claims can become stale after edits; full goal is broader than current MVP. |
| Improvements made | Added this canonical ledger; morning report now references current test count and distinct built/gated slices; completion audit remains explicit that full deployable system is not complete. |
| Validation | Report section search plus full Rust gates. |
| Safety boundaries | No external credential or AI API use; no network dependency for review. |
| Open gates | Keep updating this ledger whenever a new behavior slice is completed. |
| Next owner/action | Convert this into a machine-checkable checklist once the repo becomes a Git project. |

## Slice: Source File Scanning And Sanitized Launch Context

| Field | Value |
| --- | --- |
| Scope | Added `whoathere-source`; source scans for `.npmrc`, pip config, npm lockfiles, and requirements files; Vault-origin validation; sanitized npm/pip launch-context planning; `protect --workspace --vault-origin` source gate; expanded redaction and safer readonly execution planning. |
| Pass count | 5 |
| Pass artifacts/evidence | Pass 1: source-scanner reviewer flagged that standalone scanners would not protect installs and substring scanning would miss critical cases. Pass 2: launch-context reviewer flagged missing sanitized npm/pip envelope, ambient env inheritance, and raw secret logging. Pass 3: implementation tests covered unsafe source files, invalid Vault origins, generated context, private config materialization, and no-runner source blocks. Pass 4: full Rust gates and CLI smoke checks exercised clean and unsafe source fixtures. Pass 5: post-implementation review fixed split-argv secret redaction, Unicode-escaped lockfile URL detection, bare requirements path detection, duplicate VCS findings, and over-redacted safe env-name metadata; gates were rerun. |
| Risks found | Scanner output was initially diagnostic-only; package sources could bypass `protect`; package-manager args and context display could leak secrets; relative readonly execution could resolve through user-controlled PATH/shims; registry steering could be mistaken for network containment; display output duplicated one VCS finding and obscured safe scrubbed-env names; Unicode-escaped lockfile URLs and bare requirements paths could evade earlier source detection. |
| Improvements made | `protect` now evaluates workspace source scans before runner planning and returns deny with `execution_reason=source_scan_blocked`; source parsers handle npm config, pip config, JSON string pairs including Unicode escapes in lockfiles, requirements flags/direct refs/VCS/local paths/includes; Vault origins reject public registries and credentialed URLs; npm/pip context plans clear ambient env and point only at Vault; redaction catches npm/pip/proxy/cloud-token markers, split sensitive argv values, and URL credentials; readonly child execution now requires an absolute package-manager path and `env_clear`; display now distinguishes safe metadata names from sensitive values. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 95 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; smoke checks for config `vault_url_set=true`, clean source workspace pass, unsafe source workspace expected exit 20, npm/pip context planning, invalid public Vault origin expected exit 20, and `protect --workspace --vault-origin` refusing unsafe source files before runner planning. |
| Safety boundaries | Sanitized npm/pip config is compatibility plumbing only, not an egress boundary; install/build/import execution remains refused; no package installs, lifecycle scripts, Python build backends, package imports, public registry fetches, or malware execution were performed. |
| Open gates | Real execution still needs OS containment, independent egress enforcement, source-policy admission by package identity, ambient environment policy decisions, broader lockfile schema support, real temp-runtime lifecycle, and signed config/policy distribution. |
| Next owner/action | Completed by the `Containment Launch Planning And No-Spawn Gates` slice; continue with real containment and egress proof objects. |

## Slice: Containment Launch Planning And No-Spawn Gates

| Field | Value |
| --- | --- |
| Scope | Added `whoathere-launch`; launch plans for npm/pip workflows; explicit source, argv, runtime, egress, containment, and execution-disabled gates; private runtime config materialization; cleanup-owned path tracking; CLI `launch plan`; `protect --execute` launch gate before runner execution. |
| Pass count | 5 |
| Pass artifacts/evidence | Pass 1: reviewed existing runner/source/sandbox boundaries and kept install execution out of the runner. Pass 2: implemented launch crate with missing Vault, unsafe source, egress, containment, all-gates, runtime materialization, and fake package-manager no-spawn tests. Pass 3: review agent flagged P0/P1 risks: sanitized config must not count as egress, no second execution path, explicit containment gate, final exit-code precedence, private runtime dirs, argv override blocking, cleanup ownership. Pass 4: hardened launch crate with argv source override refusal before materialization, 0700 runtime/home/XDG dirs, 0600 config, cleanup lease metadata, no placeholder final env/argv, and `protect --execute` launch gating. Pass 5: full workspace gates and CLI smokes confirmed launch blocks and planning behavior. |
| Risks found | Launch context could be mistaken for egress proof; a launch helper could accidentally create a second install execution path; runtime dirs initially only proved config privacy; user registry/index args could override sanitized config; `protect --execute` previously reached the generic runner gate instead of the launch gate. |
| Improvements made | `whoathere-launch` never spawns installs and `execute_launch_plan` returns a local refusal unless `execution_allowed` is true, which remains false for install workflows; launch plans block missing/invalid Vault origin, source-scan findings, argv source overrides, missing egress proof, and missing containment proof; runtime materialization uses private directories/config and tracks cleanup-owned paths; `protect --execute --workspace --vault-origin npm -- ci` now reports `launch_plan_blocked` before runner planning. |
| Validation | At the time of this slice: `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 110 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; smoke checks for boolean placeholder gates. The boolean behavior is superseded by the typed proof-object slice below. |
| Safety boundaries | No npm/pip install execution; no lifecycle scripts; no Python build backend/import execution; no registry fetch; no network containment claim from npm/pip config steering; `--egress-enforced` and `--containment-available` are explicit proof placeholders for this slice, not real OS enforcement. |
| Open gates | Boolean proof replacement completed by the typed proof-object slice below; remaining gates are trusted Linux/macOS containment providers, independent egress enforcement providers, cleanup execution, policy-admitted package identity, and fake-binary integration around the final runner handoff. |
| Next owner/action | Build the first concrete OS containment and egress proof-provider path, then wire launch plans to accept only verified provider output. |

## Slice: Typed Proof Objects And Pre-Materialization Proof Gates

| Field | Value |
| --- | --- |
| Scope | Replaced launch-plan boolean placeholders with typed `ContainmentProof` and `EgressProof` objects; made CLI assertion flags diagnostic-only; required verified Vault-only egress and verified high-risk containment before runtime materialization; added proof-focused no-spawn tests. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: reviewer flagged build breakage and the risk that operator flags could be mistaken for verified controls. Pass 2: implemented proof-status types, operator/asserted/verified constructors, mandatory non-Vault denial checks, and launch proof gates. Pass 3: updated CLI rendering/tests so `--containment-available` and `--egress-enforced` map only to `OperatorAsserted` and still block high-risk installs. Pass 4: refined platform backend labeling, preserved safe launch context in blocked plans, and reran focused validation. |
| Risks found | Boolean gates made it too easy to treat human assertion as enforcement; unverified proof failures previously happened after runtime materialization; egress proof construction could omit required negative checks; CLI output could imply that asserted controls were sufficient. |
| Improvements made | `ContainmentProof` now distinguishes `Missing`, `OperatorAsserted`, and `Verified`; verified egress requires the configured Vault host to be allowed and mandatory public/metadata/loopback non-Vault destinations to be denied; launch planning blocks on proof verification before runtime file creation; CLI renders proof status/reason fields; protected install paths use missing proofs until a trusted provider exists. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 115 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; CLI smokes for missing proof, operator-asserted proof, `protect --execute`, and absent runtime dirs. |
| Safety boundaries | No npm/pip install execution; no lifecycle scripts; no Python build backend/import execution; no registry fetch; operator assertion flags are not verification and cannot make high-risk install workflows planned. |
| Open gates | Implement trusted OS containment proof providers; implement an independent network egress proof provider; execute cleanup leases; bind proof provenance to policy/audit; add integration harnesses that use fake package-manager binaries under verified local containment only. |
| Next owner/action | Build a Linux/macOS proof-provider spike that can produce `Verified` proofs from real capability checks without enabling real package-manager installs. |

## Slice: Proof Provider SPI, Provenance, And Replay Hardening

| Field | Value |
| --- | --- |
| Scope | Added sandbox-owned proof provider SPI scaffolding, proof provenance summaries, launch subject binding, egress policy scope, opaque proof structs, and test-only verified proof helpers behind a non-default feature. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: read-only provider-SPI reviewer identified missing provenance, subject binding, and egress scope. Pass 2: adversarial reviewer identified forgeable public proof structs and replay/stale proof risks. Pass 3: implemented proof subject/provenance/mechanism/scope, same-subject launch gate, wrong-Vault and mismatch tests, and CLI proof summaries. Pass 4: made proof structs opaque to external crates, hid verified proof helpers behind `whoathere-sandbox/test-support`, and verified normal builds do not expose those helpers. |
| Risks found | Public proof fields allowed external struct-literal forging; verified constructors were callable outside the sandbox crate; egress proof checked sample destinations but did not encode default-deny scope; containment and egress proofs could be replayed across launches; CLI/protect output did not expose enough proof provenance for audit. |
| Improvements made | `ProofSubject` binds proofs to a launch; `EgressPolicyScope::DefaultDenyExceptConfiguredVault` is required for verified egress; `ProofProvenance` records provider id/version, platform, mechanism, trust, and evidence count; launch plans require egress and containment proofs to share the same bound subject; proof structs expose accessors instead of public fields; verified proof minting is sandbox-internal, with only test-support helpers available for unit tests. |
| Validation | Normal `cargo build --manifest-path whoathere/Cargo.toml -p whoathere-cli -p whoathere-launch -p whoathere-sandbox` without warnings; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 121 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; CLI smokes for operator-asserted launch proof summaries, `protect --execute`, and absent runtime dir. |
| Safety boundaries | No npm/pip install execution; no lifecycle scripts; no Python build backend/import execution; no registry fetch; no fake provider is selectable from CLI/config/env; normal builds do not include the `test-support` verified proof helpers. |
| Open gates | Real Linux/macOS providers must return verifiable evidence, not self-certified proof; add TTL/context hashes/rule-generation IDs; persist proof summaries into audit events; implement cleanup execution; add compile-fail tests if the project adopts a trybuild-style test harness. |
| Next owner/action | Build a Linux proof-evidence collector that remains fail-closed until a sandbox-owned verifier can prove namespace/seccomp/Landlock and default-deny egress state for a launch subject. |

## Slice: Proof Freshness And Context Metadata

| Field | Value |
| --- | --- |
| Scope | Added proof validity-window, context-hash, and rule-generation metadata to provenance; launch plans now reject expired or not-yet-valid verified proofs before runtime materialization; CLI/protect output includes proof freshness summaries. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: implemented deterministic freshness metadata without wall-clock dependency. Pass 2: added expired test-support proofs and launch stale-proof acceptance test. Pass 3: added CLI output fields and focused validation. |
| Risks found | Same-subject proofs could still be stale or generated for an outdated enforcement rule set; CLI summaries lacked the metadata needed to audit freshness decisions. |
| Improvements made | `ProofProvenance` now records `verified_at_unix_seconds`, `expires_at_unix_seconds`, `context_hash`, and `rule_generation_id`; launch requests include a deterministic proof validation time; launch plans block `proof_expired_or_not_yet_valid`; CLI output renders proof validity fields. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 122 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; CLI smoke for operator-asserted freshness fields and absent runtime dir. |
| Safety boundaries | No real package execution; freshness fields are metadata/gating only and do not claim real OS enforcement; expired proof helpers remain test-support only. |
| Open gates | Replace placeholder context hashes and rule generation IDs with hashes of sanitized launch context, source scan summary, and provider-enforced rule generation; add wall-clock validation once provider evidence includes trusted timestamps. |
| Next owner/action | Implement deterministic sanitized-context/source-scan hash generation and require provider evidence to bind to those hashes before proof verification. |

## Slice: Deterministic Launch And Source Fingerprints

| Field | Value |
| --- | --- |
| Scope | Added deterministic fingerprints for sanitized launch context and source scan summaries; this slice originally used `fnv64:` summaries and was later superseded by SHA-256 binding in the provider verification challenge slice. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: selected stable owned fields from `SanitizedExecutionContext` and `SourceScanReport`. Pass 2: implemented deterministic FNV-1a fingerprints in `whoathere-launch` and stored them on launch plans. Pass 3: rendered hashes in CLI/protect output and pinned them with launch/CLI assertions. |
| Risks found | Proof provenance had placeholder context/rule IDs but no deterministic launch/source binding output; future provider evidence could not be tied cleanly to the sanitized context or source scan state. |
| Improvements made | Launch plans now carry `launch_context_hash` and `source_scan_hash`; source findings are sorted before hashing to keep output deterministic; CLI `launch plan` renders `launch_context_hash` and `source_scan_hash`; `protect --execute` renders `launch_plan_context_hash` and `launch_plan_source_scan_hash`. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 122 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; CLI smoke for rendered `launch_context_hash`/`source_scan_hash` and absent runtime dir. |
| Safety boundaries | Hashes are binding metadata only and are not cryptographic proof; no package execution, no registry fetch, no lifecycle script, and no provider enforcement claim was added. |
| Open gates | FNV replacement and proof provenance context binding are complete in later slices; remaining work is to bind source-scan hashes into real provider evidence and signed production attestations. |
| Next owner/action | Add proof-to-context hash matching gates once real provider evidence can include context hashes. |

## Slice: Guarded Runtime Cleanup Execution

| Field | Value |
| --- | --- |
| Scope | Added `cleanup_runtime_plan` for launch-owned runtime paths with strict refusal rules for non-WhoaThere temp roots and paths outside the runtime root. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: designed cleanup to use existing cleanup lease metadata and reverse ownership order. Pass 2: implemented guarded deletion with explicit removed/refused path reporting. Pass 3: added tests for successful WhoaThere temp cleanup and refusal of non-WhoaThere temp roots. |
| Risks found | Cleanup code can become destructive if it trusts arbitrary runtime paths; deleting parent dirs before children can create partial cleanup failures. |
| Improvements made | Cleanup requires the runtime root to live under the OS temp directory and have a `whoathere` filename prefix; every owned path must be under the runtime root; cleanup deletes in reverse ownership order; result reporting separates removed, refused, and already-absent paths. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 124 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Cleanup only removes paths that WhoaThere materialized under a guarded temp root; it refuses arbitrary roots and paths outside the runtime root. |
| Open gates | Wire cleanup execution into CLI command flow and audit events; decide policy for user-specified non-temp runtime dirs before allowing cleanup outside temp. |
| Next owner/action | Add CLI cleanup/reporting commands and audit records for cleanup results, still without enabling package-manager installs. |

## Slice: Proof And Cleanup Audit Binding

| Field | Value |
| --- | --- |
| Scope | Added structured audit summaries for proof provenance, launch/source hashes, and cleanup state; added `whoathere launch audit` with optional JSONL persistence via `--audit-path`. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected audit/launch/CLI boundaries and kept audit independent of raw provider evidence. Pass 2: added `AuditProofSummary` and `AuditCleanupSummary` with JSONL rendering and redaction tests. Pass 3: added CLI `launch audit` render path sharing launch-plan construction, plus persisted JSONL via `--audit-path`. Pass 4: added parse/output/persistence tests and focused gates. |
| Risks found | Audit records previously lacked proof status/trust/provider details, launch/source fingerprints, and cleanup state; storing raw evidence could leak secrets or firewall details; audit writing must not imply install execution. |
| Improvements made | Audit records can now include redacted launch/source hashes, proof kind/status/subject/provider/version/platform/mechanism/trust/expiry/context/rule-generation/evidence-count/reason, and cleanup attempted/removed/refused counts; `launch audit` persists JSONL when `--audit-path` is provided; operator-asserted installs still deny and do not materialize runtime. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 127 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; CLI smoke wrote `/private/tmp/whoathere-launch-audit-smoke.jsonl` with deny/proof/cleanup summaries and did not create the requested runtime dir. |
| Safety boundaries | Audit output records summaries and counts only; no raw proof evidence, secrets, package payloads, package-manager installs, lifecycle scripts, registry fetches, or cleanup execution are triggered by `launch audit`. |
| Open gates | Add cleanup execution reports and real provider evidence verification once providers exist. |
| Next owner/action | Add explicit cleanup/reporting command or start fake-provider integration harness. |

## Slice: Protect Audit Append

| Field | Value |
| --- | --- |
| Scope | Added optional `protect --audit-path` persistence for deny decisions from the source-scan gate and launch-plan gate. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected protect command flow and confirmed policy, source, and launch gates block before runner planning. Pass 2: added `--audit-path` parsing and command state without changing non-audit protect behavior. Pass 3: reused the existing launch audit record builder for launch-gate denies and added a source-gate audit record using displayed reason codes. Pass 4: added persisted JSONL tests for source-gate and launch-gate denies plus focused CLI/audit crate gates. |
| Risks found | `protect --execute` denied high-risk workflows but did not persist deny evidence; adding audit writes must not imply execution, materialize runtime, leak token-like argv, or bypass source-scan precedence. |
| Improvements made | `protect --audit-path` now appends redacted source-scan deny records with `protect-source-gate` event IDs and launch-gate deny records with launch/source hashes, proof summaries, and `runtime_not_materialized` cleanup summaries. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli` with 35 CLI tests; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-audit` with 6 audit tests; `cargo test --manifest-path whoathere/Cargo.toml` with 130 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; protect-audit smoke wrote `/private/tmp/whoathere-protect-audit-smoke.jsonl` with deny/proof/cleanup summaries. |
| Safety boundaries | `protect --audit-path` appends JSONL only on local deny paths; it does not execute npm/pip, run lifecycle scripts, invoke Python backends/imports, fetch registries, materialize launch runtime after failed proof gates, or run cleanup. |
| Open gates | Decide whether audit append failures should fail closed for high-risk CI; add production audit sink/auth/storage; add cleanup reporting once explicit cleanup commands exist. |
| Next owner/action | Start fake-provider integration harness for Linux/macOS proof-provider spikes. |

## Slice: Cleanup Manifest Reporting

| Field | Value |
| --- | --- |
| Scope | Added durable cleanup manifests for materialized launch runtime plans plus `launch cleanup --manifest <path>` report/execute command behavior. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected `RuntimePlan` and existing guarded cleanup boundaries. Pass 2: added private cleanup manifest writing/loading and included the manifest in cleanup-owned paths. Pass 3: hardened cleanup path checks to reject parent-directory escape components before deletion. Pass 4: added CLI report-only, execute, and cleanup audit tests plus focused launch/CLI gates. |
| Risks found | Cleanup previously worked only from in-memory runtime plans, making operator cleanup non-reproducible; prefix-only path checks could be ambiguous around `..` components; a cleanup command must be non-mutating by default and must not imply package-manager execution. |
| Improvements made | Runtime materialization writes `.whoathere-cleanup.manifest`; `launch cleanup --manifest` reports scope by default; `--execute` performs guarded cleanup and can append a cleanup audit summary; cleanup refuses non-WhoaThere temp roots, paths outside runtime, and parent-directory escape components. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-launch -p whoathere-cli` with 13 launch tests and 38 CLI tests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`; `cargo test --manifest-path whoathere/Cargo.toml` with 134 unit tests plus doctests. |
| Safety boundaries | Cleanup manifests only describe local WhoaThere runtime paths; report mode does not mutate; execute mode never invokes npm/pip, lifecycle scripts, Python backends/imports, registries, or launch runtime materialization. |
| Open gates | Bind cleanup manifests into future real-provider evidence; decide production cleanup audit retention and audit-sink failure policy. |
| Next owner/action | Bind proof provenance context hashes to launch/source hashes and start real Linux/macOS provider collectors. |

## Slice: Test-Only Proof Provider Harness

| Field | Value |
| --- | --- |
| Scope | Added a `whoathere-sandbox/test-support` fake provider harness implementing the real `ProofProvider` trait, plus launch-level integration coverage. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected proof constructors and provider SPI to keep verified proof minting outside normal builds. Pass 2: added `TestOnlyProofProvider` behind `test-support` with same-subject containment proof and default-deny-except-Vault egress proof generation. Pass 3: added sandbox tests for same-subject verified proofs and mandatory public/metadata/loopback egress denials. Pass 4: added launch integration test proving fake verified proofs can materialize private runtime/cleanup manifest while install execution remains disabled. |
| Risks found | Test-only verified proof helpers existed as raw constructors rather than a provider-contract harness; future OS providers needed a reusable integration shape without opening production proof minting or package-manager execution. |
| Improvements made | The fake harness implements `ProofProvider`, appends mandatory egress denial probes, binds proofs to a launch subject, and validates the full launch planning path through runtime materialization while `execute_launch_plan` still refuses install execution. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all`; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-launch` with 15 sandbox tests and 14 launch tests; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 136 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | The provider is behind non-default `test-support`; normal builds cannot call it. It does not run npm/pip, lifecycle scripts, Python backends/imports, registry fetches, or OS sandbox commands. |
| Open gates | Replace fake provider with real Linux/macOS collectors that emit independently verifiable evidence while preserving fail-closed defaults. |
| Next owner/action | Bind source-scan hashes into provider evidence or audit policy where needed, then begin real provider collectors. |

## Slice: Proof Context Hash Binding

| Field | Value |
| --- | --- |
| Scope | Bound verified containment and egress proof provenance to the deterministic launch-context hash before runtime materialization. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected launch proof validation order and test-support proof provenance. Pass 2: added context-aware trusted provider provenance and context-aware test-support proof helpers. Pass 3: added `preview_launch_context_hash` and a `proof_context_hash_mismatch` launch gate after subject checks and before freshness/runtime materialization. Pass 4: updated fake-provider/launch tests to prove context-bound success and stale-context denial. |
| Risks found | Same-subject fresh proofs could still be replayed against a different sanitized launch context if provider provenance was not bound to the exact context hash. |
| Improvements made | Launch planning now requires containment and egress proof provenance context hashes to equal the current deterministic launch context hash; mismatches deny before runtime materialization. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all`; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-launch` with 15 sandbox tests and 15 launch tests; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 137 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | The binding does not enable install execution, run npm/pip, execute scripts/imports, fetch registries, or claim OS containment; it only tightens proof validation before runtime creation. |
| Open gates | Decide whether source-scan hash should also bind to provider evidence; implement real Linux/macOS proof collectors under the same provider contract. |
| Next owner/action | Replace provider skeletons with real Linux/macOS collectors while preserving fail-closed defaults. |

## Slice: Fail-Closed Local Provider Skeletons

| Field | Value |
| --- | --- |
| Scope | Added normal-build Linux/macOS local proof provider skeletons that implement `ProofProvider` but cannot verify containment or egress yet. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: added provider structs with explicit platform/mechanism provenance and unimplemented reason codes. Pass 2: kept both providers `Missing`/trust-none/no-evidence for all proofs. Pass 3: added sandbox tests locking in fail-closed behavior for Linux namespace/packet-filter and macOS VM/packet-filter mechanisms. |
| Risks found | Real-provider work needs stable entrypoints, but skeleton entrypoints must not accidentally satisfy high-risk proof gates or create operator confusion. |
| Improvements made | `LinuxLocalProofProvider` and `MacosLocalProofProvider` now provide concrete, tested extension points for future collectors while preserving fail-closed defaults. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all`; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox` with 17 sandbox tests; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 139 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Skeletons do not execute OS sandbox commands, packet-filter changes, npm/pip, lifecycle scripts, Python backends/imports, or registry fetches; they return unavailable proofs only. |
| Open gates | Implement real Linux/macOS evidence collection behind these provider contracts; add platform-gated integration tests that still never run real package installs. |
| Next owner/action | Start Linux provider evidence collector design/implementation. |

## Slice: Read-Only Provider Capability Evidence

| Field | Value |
| --- | --- |
| Scope | Added read-only local capability evidence collection to the Linux/macOS provider skeletons while keeping proof status `Missing`. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: added platform-specific probe functions that only read filesystem/procfs state. Pass 2: wired probe evidence into Linux/macOS unavailable proof provenance. Pass 3: updated provider tests to require stable evidence shape while preserving denied proof semantics. |
| Risks found | Provider skeletons with no diagnostic evidence were hard to evolve into real collectors; evidence gathering must not mutate OS state or be mistaken for verified containment/egress. |
| Improvements made | Linux probes record `/proc` status/namespace/cgroup/userns/Landlock signals when available; macOS probes record framework/tool path presence for Hypervisor, Virtualization, EndpointSecurity, NetworkExtension, `sandbox-exec`, and `pfctl`; proofs still use trust `None`, status `Missing`, and unimplemented reason codes. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all`; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox` with 17 sandbox tests; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 139 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Probes are read-only filesystem/procfs checks. They do not execute commands, modify namespaces, load kernel features, change packet filters, run package managers, fetch registries, or enable high-risk proof gates. |
| Open gates | Convert selected read-only signals into real verified evidence only after enforcement providers can prove containment and default-deny egress independently. |
| Next owner/action | Expose provider diagnostics in the CLI, then design real Linux containment collector steps. |

## Slice: CLI Provider Diagnostics

| Field | Value |
| --- | --- |
| Scope | Added `whoathere evidence providers` to render fail-closed Linux/macOS provider status and read-only evidence signals. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: reused the existing `evidence` command family instead of adding a new top-level namespace. Pass 2: rendered containment/egress status, reason, mechanism, trust, evidence counts, and probe signals for both local providers. Pass 3: added CLI tests proving provider diagnostics stay `Missing` and include read-only probe markers. |
| Risks found | Provider evidence must be inspectable for operators, but exposing it must not imply that enforcement is active or verified. |
| Improvements made | The CLI now reports provider diagnostics while keeping proof status `Missing`, trust `None`, and unimplemented reason codes visible. |
| Validation | `cargo fmt --manifest-path whoathere/Cargo.toml --all`; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli -p whoathere-sandbox` with 39 CLI tests and 17 sandbox tests; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 140 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | The command only renders local read-only evidence already collected by fail-closed providers; it does not execute commands, mutate OS state, run package managers, fetch registries, or create verified proofs. |
| Open gates | Add machine-readable provider diagnostics once audit/admin APIs exist; build real Linux/macOS collectors behind these visible provider contracts. |
| Next owner/action | Start Linux containment collector design/implementation. |

## Slice: Linux Containment Readiness Model

| Field | Value |
| --- | --- |
| Scope | Added a structured Linux containment readiness model derived from read-only provider evidence and rendered it in `whoathere evidence providers`. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: parsed existing Linux evidence signals into stable booleans/options and missing-primitive reason codes. Pass 2: preserved fail-closed semantics by keeping `proof_verification_enabled=false` regardless of readiness. Pass 3: rendered readiness in CLI diagnostics and added sandbox/CLI assertions. |
| Risks found | Raw probe strings made it hard to know which Linux containment prerequisites were missing; readiness could be confused with verified enforcement if it were not explicitly separated from proof verification. |
| Improvements made | `LinuxContainmentReadiness` now reports procfs status, namespace observations, cgroup visibility, no-new-privs, seccomp mode, unprivileged user namespace clone status, Landlock ABI version, required primitive presence, and reason codes. CLI diagnostics now include a `provider_readiness provider=linux` line while keeping proof status `Missing`. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-cli` with 39 CLI tests and 19 sandbox tests; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 142 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Readiness is diagnostic only. It does not execute commands, create namespaces, install seccomp/Landlock rules, modify cgroups, alter packet filters, run npm/pip, fetch registries, or create verified proofs. |
| Open gates | Implement a real Linux verifier that proves same-subject containment and default-deny egress from independently checked enforcement state; decide whether Landlock is mandatory for specific profiles or an enhancement reason code. |
| Next owner/action | Start the Linux containment verifier design around namespace/seccomp/cgroup/Landlock evidence while retaining fail-closed defaults until verification is real. |

## Slice: macOS Containment Readiness Model

| Field | Value |
| --- | --- |
| Scope | Added a structured macOS containment readiness model derived from read-only provider evidence and rendered it in `whoathere evidence providers`. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: parsed existing macOS framework/tool path probes into VM isolation, egress-control, telemetry, and availability fields. Pass 2: preserved beta containment and fail-closed semantics by keeping `proof_verification_enabled=false`. Pass 3: rendered readiness in CLI diagnostics and added sandbox/CLI assertions. |
| Risks found | macOS framework/tool presence can be mistaken for entitlement, policy, or runtime enforcement; beta VM containment must stay visible until real VM/egress proofs exist. |
| Improvements made | `MacosContainmentReadiness` now reports Hypervisor, Virtualization, EndpointSecurity, NetworkExtension, `sandbox-exec`, and `pfctl` availability; derives VM isolation, egress-control, telemetry, required primitive, beta, and reason-code fields; CLI diagnostics now include a `provider_readiness provider=macos` line while keeping proof status `Missing`. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-cli` with 39 CLI tests and 21 sandbox tests; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 144 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Readiness is diagnostic only. It does not create VMs, request entitlements, load EndpointSecurity or NetworkExtension clients, modify packet filters, execute `sandbox-exec`/`pfctl`, run npm/pip, fetch registries, or create verified proofs. |
| Open gates | Implement a real macOS VM containment helper and egress verifier with entitlement/runtime checks; decide which telemetry primitives are mandatory versus advisory for beta containment. |
| Next owner/action | Start the macOS VM/egress verifier design while retaining beta labeling and fail-closed defaults until verification is real. |

## Slice: Machine-Readable Provider Diagnostics

| Field | Value |
| --- | --- |
| Scope | Added `whoathere evidence providers --json` as a schema-versioned, redacted provider diagnostics output for automation. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: extended CLI parsing with a JSON flag while preserving the default text renderer. Pass 2: hand-rendered deterministic JSON without adding dependencies, including explicit string escaping. Pass 3: added tests for parser behavior, proof-disabled readiness content, and JSON escaping. |
| Risks found | Automation should not scrape human text output; JSON diagnostics must not leak raw secrets or imply proof verification. |
| Improvements made | JSON output now includes provider id/platform, containment and egress status/reason/mechanism/trust/evidence count, Linux/macOS readiness objects, reason-code arrays, and redacted containment/egress evidence signals. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli` with 41 CLI tests; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers --json`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 146 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | The JSON command only renders existing fail-closed provider diagnostics. It does not execute commands, mutate OS state, run package managers, fetch registries, create verified proofs, or alter launch gate behavior. |
| Open gates | Replace hand-rendered JSON with a typed serialization layer if/when serde is accepted; publish a stable diagnostics schema once admin/audit APIs consume it. |
| Next owner/action | Use JSON diagnostics in provider-contract integration tests for real Linux/macOS verifier work. |

## Slice: Verifier Prerequisite Plan Contracts

| Field | Value |
| --- | --- |
| Scope | Added structured local verifier prerequisite plans for Linux and macOS and included them in JSON provider diagnostics. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: added a reusable `LocalVerificationPlan` contract in the sandbox crate. Pass 2: derived Linux and macOS plans from readiness models without enabling proof verification. Pass 3: rendered plan objects in `evidence providers --json` and added sandbox/CLI assertions. |
| Risks found | Readiness alone can be mistaken for permission to verify or execute; future verifier work needs explicit required checks and blocking reason codes before any OS mutation is introduced. |
| Improvements made | Linux plans list namespace, no-new-privs, seccomp, cgroup, Landlock, Vault-only egress, and launch-context binding checks. macOS plans list VM boundary, mount policy, generated package-manager config, packet-filter/default-deny egress, telemetry, and launch-context binding checks. Both platforms keep `can_verify_now=false` until proof verification is implemented. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-cli` with 41 CLI tests and 23 sandbox tests; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers --json`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 148 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Verifier plans are contracts only. They do not create namespaces or VMs, install seccomp/Landlock rules, modify cgroups or packet filters, load telemetry clients, run npm/pip, fetch registries, create verified proofs, or alter launch gate behavior. |
| Open gates | Implement actual platform verifiers that must satisfy every required check before minting same-subject verified proofs. |
| Next owner/action | Build provider-contract tests around JSON `verification_plan` fields, then implement the first read-only-to-active Linux verifier spike. |

## Slice: Provider Readiness CI Gate

| Field | Value |
| --- | --- |
| Scope | Added explicit `evidence providers --json --require-ready` check mode that fails closed when local providers cannot verify containment and egress now. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: extended CLI parsing with `--require-ready` while preserving default diagnostics. Pass 2: added top-level JSON readiness status and stable exit-code inference. Pass 3: added a focused fail-closed CLI test and smoke-ran the command. |
| Risks found | CI needs a stable failing command for missing provider verification; human-readable diagnostics should not be scraped and default diagnostics should not unexpectedly break local inspection. |
| Improvements made | JSON diagnostics now include `require_ready`, `provider_ready`, `status`, and `exit_code`; `--require-ready` returns stable deny exit code 20 until every provider plan has `can_verify_now=true`. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli` with 42 CLI tests; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers --json --require-ready` expected exit 20; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 149 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Check mode only evaluates existing diagnostic plan fields. It does not mutate OS state, create namespaces or VMs, modify packet filters, run npm/pip, fetch registries, create verified proofs, or alter launch gate behavior. |
| Open gates | Flip this gate only after real platform verifiers can set `can_verify_now=true` based on enforced same-subject containment and default-deny egress. |
| Next owner/action | Use `--require-ready` as the acceptance gate for future provider-contract CI jobs. |

## Slice: Vault Content-Addressed Cache Object Keys

| Field | Value |
| --- | --- |
| Scope | Added validated content-addressed cache object keys for promoted Vault artifacts and exposed them in local-dev promotion output. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: added digest-to-object-key validation in `whoathere-vault-api`. Pass 2: made admission fail closed before promotion for unsafe or unsupported digests. Pass 3: carried the object key on `ServableGeneration` and rendered it in local-dev admission simulation. |
| Risks found | A broad cache key based on package metadata is not sufficient for storage object naming; unsafe digest strings could otherwise become ambiguous or path-like cache keys. |
| Improvements made | Supported digests map to `blobs/<algorithm>/<digest>` keys; unsupported algorithms, missing separators, empty values, and unsafe digest characters are rejected; promoted generations now carry the canonical cache object key. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api -p whoathere-admission -p whoathere-registry -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations complete=true`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 151 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | This slice does not fetch public registries, serve package payloads, write object storage, execute detonation jobs, or run package managers. It only validates and records promotion metadata. |
| Open gates | Add object-store persistence, hash verification against fetched bytes, immutable promotion metadata, and registry download routes that serve only promoted object keys. |
| Next owner/action | Build an in-memory quarantine-to-promoted cache object lifecycle that still uses inert bytes only. |

## Slice: In-Memory Vault Cache Lifecycle

| Field | Value |
| --- | --- |
| Scope | Added `whoathere-cache` with an inert in-memory quarantine-to-promoted object lifecycle and exposed it through local-dev cache simulations. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: added a dedicated cache crate with quarantine receipts, promoted object metadata, and fail-closed errors. Pass 2: validated object keys before quarantine and blocked empty payloads, unknown quarantine IDs, and duplicate promotions. Pass 3: added local-dev `/v1/cache-simulations` quarantine/promote routes and CLI smokes. |
| Risks found | Cache lifecycle work can accidentally become package serving or registry fetching; keeping it inert and in-memory preserves safety while still exercising state transitions. |
| Improvements made | Inert bytes can now move from quarantine to promoted storage only after cache object-key validation; promoted bytes are addressable by object key inside the in-memory store; local-dev simulation exposes `promoted=false` and `promoted=true` paths. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cache -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/cache-simulations promote=false`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/cache-simulations promote=true`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 157 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | No public registry fetches, no package serving routes, no package-manager execution, no detonation, no object-store writes, and no real package payloads. Test and simulation bytes are inert local literals only. |
| Open gates | Extend hash verification to fetched bytes and SHA-512 integrity strings, add persistent object storage, immutable promotion manifests, and package-serving routes that can only read promoted objects. |
| Next owner/action | Add a promotion manifest tying cache object key, artifact identity, evidence profile, policy version, and audit metadata together. |

## Slice: Vault Promotion Manifest Metadata

| Field | Value |
| --- | --- |
| Scope | Added schema-versioned promotion manifest metadata for servable Vault generations and exposed generation/audit identifiers in local-dev admission simulation. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: extended `ServableGeneration` with tenant id, request id, cache object key, evidence profile, policy version, and audit event id. Pass 2: added `promotion_manifest()` and assertions binding manifest fields. Pass 3: rendered generation id and audit event id in local-dev promotion output. |
| Risks found | Promoted cache objects need immutable identity/audit metadata; object keys alone are not enough to reconstruct why a package became servable. |
| Improvements made | Promotion metadata now ties generation id, tenant/request identity, artifact identity, cache object key, evidence profile, policy version, and audit event id together for future persistence. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-admission -p whoathere-registry -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations complete=true`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 157 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Metadata only. No registry fetches, package serving, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Persist promotion manifests immutably, sign or hash them, and bind them to audit/event storage and object-store writes. |
| Next owner/action | Add immutable manifest serialization once a typed JSON dependency or serialization strategy is selected. |

## Slice: Deterministic Promotion Manifest Serialization

| Field | Value |
| --- | --- |
| Scope | Added deterministic JSON serialization for promotion manifests and exposed the canonical manifest in local-dev admission promotion output. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: implemented stable field-order JSON serialization on `PromotionManifest` without adding dependencies. Pass 2: added explicit JSON string escaping tests for tenant/request/artifact fields. Pass 3: nested the canonical manifest in local-dev admission simulation and smoke-ran the output. |
| Risks found | Promotion metadata is not useful for persistence or audit if every renderer can order or escape fields differently; manifest output must not depend on broad ad hoc response formatting. |
| Improvements made | `PromotionManifest::to_canonical_json()` now emits schema version, generation id, tenant/request id, artifact identity, cache object key, evidence profile, policy version, and audit event id with deterministic ordering and escaping. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-admission -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations complete=true`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 158 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Serialization only. No registry fetches, package serving, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Add cryptographic manifest hashing/signing and immutable persistence once object storage/audit sinks exist. |
| Next owner/action | Add manifest hash binding or persistent manifest storage when a cryptographic dependency and object-store boundary are selected. |

## Slice: Cache Payload SHA-256 Verification

| Field | Value |
| --- | --- |
| Scope | Added SHA-256 payload verification before cache quarantine and updated local-dev cache simulation to use a real content-addressed digest. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: added an internal SHA-256 implementation and pinned it with the standard `abc` test vector. Pass 2: made cache quarantine reject digest mismatches and unsupported verification algorithms. Pass 3: updated local-dev inert cache simulation to use the verified SHA-256 object key and smoke-ran promotion. |
| Risks found | Validating object-key shape is not enough; a cache entry must not enter quarantine unless the payload matches its declared digest. |
| Improvements made | `InMemoryCacheStore::quarantine` now checks non-empty payload, digest/key validity, and SHA-256 payload match before issuing a quarantine receipt; mismatch, empty payload, unsafe digest, unknown quarantine, duplicate promotion, and unsupported verification algorithms all fail closed. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cache -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/cache-simulations promote=true`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 161 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Hash verification uses inert local bytes only. No public registry fetches, package serving routes, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Add SHA-512 verification for common npm/PyPI integrity strings and verify bytes fetched by a future quarantine fetcher before object-store writes. |
| Next owner/action | Add a fetch-job schema that records expected digest, fetched byte count, and verification result without performing network fetches yet. |

## Slice: Vault Fetch-Job Planning Schema

| Field | Value |
| --- | --- |
| Scope | Added non-network fetch-job planning schema and local-dev simulation for Vault quarantine fetch metadata. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: added `FetchJobRequest`, `FetchJobPlan`, and `FetchJobState` in `whoathere-vault-api`. Pass 2: validated cache object key, expected digest, HTTPS source URL, and byte limit while keeping `fetch_enabled=false`. Pass 3: exposed planned/rejected local-dev simulations and smoke-ran both paths. |
| Risks found | Fetch planning can accidentally become a network path; this slice must validate metadata without fetching or writing bytes. |
| Improvements made | Valid metadata returns `Planned` with `fetch_enabled=false`; digest mismatch, unsafe digest, zero byte limit, and non-HTTPS source metadata return fail-closed rejected plans with reason codes. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/fetch-job-simulations valid=true`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/fetch-job-simulations digest_mismatch=true`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 167 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Planning only. No public registry fetches, package serving, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Implement actual fetch worker in an isolated egress-controlled plane, verify fetched bytes against expected digest, write only verified bytes to quarantine storage, and bind fetch results to promotion manifests. |
| Next owner/action | Add fetch result/admission binding or begin an isolated fetch worker skeleton that cannot run by default. |

## Slice: Vault Fetch-Result Admission Binding

| Field | Value |
| --- | --- |
| Scope | Added non-network fetch-result binding and required that binding before Vault admission can promote a generation. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: read-only subagent pressure test identified evidence-only promotion, tenant/request drift, digest/key mismatch, and accidental serving risks. Pass 2: added `FetchJobResult` and `FetchResultBinding` with tenant, admission request, artifact, digest, cache key, byte, quarantine, network-attempt, and audit metadata checks. Pass 3: made `AdmissionController` require a verified fetch binding before promotion and record fetch provenance in `PromotionManifest`. Pass 4: updated local-dev admission simulations for successful binding, missing binding, and mismatched binding smoke paths. |
| Risks found | Complete evidence alone was sufficient to promote; result metadata without request id could drift across tenants or requests; placeholder digest metadata could be promoted without a byte-verification boundary; future registry routes must not serve packages from artifact URLs without exact promoted cache lookup. |
| Improvements made | Complete evidence without fetch binding now fails closed with `verified_fetch_result_missing`; mismatched verified digest or cache key fails closed with `fetch_result_not_admission_ready`; promotion manifests now include fetch job id, quarantine id, byte length, byte limit, and fetch audit event id. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api -p whoathere-admission -p whoathere-registry -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations complete=true`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations 'complete=true&fetch_missing=true'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations 'complete=true&fetch_mismatch=true'`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 173 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Binding only. No public registry fetches, package serving routes, package-manager execution, detonation, object-store writes, or real package payloads. Local-dev uses inert literal bytes already covered by SHA-256 cache verification. |
| Open gates | Canonicalize digest support around verified algorithms, bind evidence bundles to artifact/profile identity, persist quarantine/promoted objects, and implement actual isolated fetch workers under egress control. |
| Next owner/action | Add strict digest canonicalization or evidence-bundle artifact/profile binding before package-serving routes are introduced. |

## Slice: Canonical SHA-256 Digest Enforcement

| Field | Value |
| --- | --- |
| Scope | Restricted content-addressed cache object keys to canonical lowercase `sha256:<64 hex>` digests and removed placeholder digest use from promotable simulations. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: searched all placeholder digest fixtures and mapped affected API/cache/admission/registry/local-dev tests. Pass 2: changed `cache_object_key_for_digest` to reject unsupported algorithms, short digests, non-canonical uppercase/non-hex values, and unsafe path-like characters. Pass 3: migrated admission, registry, cache, CLI Vault simulation, and local-dev fixtures to real SHA-256 digest values and smoke-ran canonical/invalid paths. |
| Risks found | Permissive digest strings could produce object keys for values cache verification cannot actually prove, especially `sha512` and placeholder `sha256:abc` metadata. |
| Improvements made | Object key issuance now aligns with implemented cache verification: only lowercase SHA-256 hex digests are promotable. Invalid digest metadata fails before quarantine, fetch planning, or admission promotion. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-api -p whoathere-cache -p whoathere-admission -p whoathere-registry -p whoathere-vault-dev -p whoathere-cli`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault simulate --complete`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/fetch-job-simulations valid=true`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/fetch-job-simulations invalid_digest=true`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/cache-simulations promote=true`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 173 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Validation only. No public registry fetches, package serving routes, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Add SHA-512 parsing and verification only after cache verification, package-manager integrity parsing, and promotion manifests support it end to end. |
| Next owner/action | Bind evidence bundles to artifact/profile identity or add persistent quarantine metadata before package-serving routes are introduced. |

## Slice: Evidence Bundle Profile Binding

| Field | Value |
| --- | --- |
| Scope | Bound admission evidence to the selected evidence profile and rejected duplicate or unexpected job results before promotion decisions. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: inspected `EvidenceBundle::mandatory_jobs_passed` and admission usage, confirming profile id/version and duplicate results were not validated. Pass 2: added `EvidenceBundle::validate_for_profile` with reason-coded failures for profile id mismatch, profile version mismatch, unexpected job result, and duplicate job result. Pass 3: required validation in `AdmissionController::decide` and exposed profile-mismatch and duplicate-result local-dev denial paths. |
| Risks found | A generic set of passed job kinds could satisfy mandatory evidence for the wrong profile; duplicate/conflicting job results could hide a failed check behind a passed one. |
| Improvements made | Admission now fails closed with `evidence_profile_id_mismatch`, `evidence_profile_version_mismatch`, `evidence_unexpected_job_result`, or `evidence_duplicate_job_result` before any manual-review or auto-allow verdict. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-evidence -p whoathere-admission -p whoathere-vault-dev -p whoathere-cli`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations 'complete=true&evidence_profile_mismatch=true'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations 'complete=true&evidence_duplicate=true'`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 179 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Evidence metadata validation only. No public registry fetches, package serving routes, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Bind evidence results to artifact digest/cache key and scanner/detonator job ids once those job records exist. |
| Next owner/action | Add tenant-aware generation keys or persistent quarantine metadata before package-serving routes are introduced. |

## Slice: Admission Tenant And Request Identity

| Field | Value |
| --- | --- |
| Scope | Made admission request registration fail closed on conflicting request-id reuse and made servable generation lookup tenant-aware. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: mapped all `request` and `servable` callers and confirmed request-id reuse was silently ignored while generation keys were artifact-only. Pass 2: changed `AdmissionController::request` to return `Result` and reject conflicting metadata with `admission_request_id_conflict`. Pass 3: keyed promoted generations by tenant plus artifact and updated CLI/local-dev callers and admission tests. |
| Risks found | A duplicate request id with different tenant/artifact metadata could be silently treated as the original request; artifact-only generation keys could bleed promoted state across tenants. |
| Improvements made | Request registration is explicit and fail-closed; same-artifact promotions for different tenants are isolated; generation lookup requires tenant id. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-admission -p whoathere-vault-dev -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 181 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Admission identity state only. No public registry fetches, package serving routes, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Add persistent tenant namespace constraints and authz checks before exposing any production Vault API. |
| Next owner/action | Add persistent quarantine metadata or a production admission API contract with tenant-authenticated request IDs. |

## Slice: Registry Metadata URL Encoding

| Field | Value |
| --- | --- |
| Scope | Hardened registry metadata rendering so package names, versions, and digest fragments are encoded for URLs while display text remains escaped separately. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: inspected npm packument and PyPI Simple rendering and identified raw package names in path components. Pass 2: added local percent-encoding for URL components and expanded JSON control-character escaping. Pass 3: added scoped npm and special-character PyPI tests without adding package-serving routes. |
| Risks found | Future package-serving routes could accidentally trust raw artifact names in rendered URLs, especially scoped npm packages with `/`, spaces, `+`, or HTML-sensitive characters. |
| Improvements made | npm tarball URL components are percent-encoded; PyPI href path and fragment components are percent-encoded; visible HTML text remains escaped independently. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-registry`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 183 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Metadata rendering only. No public registry fetches, package serving routes, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Implement exact promoted-cache lookup and authz before any tarball/file response route is introduced. |
| Next owner/action | Add persistent quarantine/promoted object metadata or production API authz before serving package bytes. |

## Slice: Exact Promoted Cache Lookup

| Field | Value |
| --- | --- |
| Scope | Added an exact promoted-byte lookup contract requiring requested artifact identity and cache object key to match the promoted cache object. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: inspected `InMemoryCacheStore` and confirmed promoted bytes could be read by object key alone. Pass 2: added `promoted_bytes_for_artifact` with fail-closed errors for key mismatch, missing promoted object, and artifact mismatch. Pass 3: added tests for exact success and mismatch/not-found failures. |
| Risks found | A future serving route using only cache object key lookup could serve bytes for the wrong artifact metadata or tenant context. |
| Improvements made | Future serving paths now have a stricter cache API that binds artifact identity to the promoted object key before returning bytes. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cache`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 185 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | In-memory lookup only. No public registry fetches, package serving routes, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Bind this lookup to tenant authz and immutable promotion manifests before exposing any package byte route. |
| Next owner/action | Add production admission/cache API contracts or persistent object metadata. |

## Slice: Evidence Subject Artifact Binding

| Field | Value |
| --- | --- |
| Scope | Bound evidence bundles to the artifact digest and cache object key they describe, and required admission to compare those fields before promotion. |
| Pass count | 3 |
| Pass artifacts/evidence | Pass 1: added `artifact_digest` and `cache_object_key` to `EvidenceBundle`. Pass 2: added admission checks for evidence subject digest/cache-key mismatch before manual-review or auto-allow decisions. Pass 3: updated CLI/local-dev evidence construction and added local-dev subject-mismatch denial smoke coverage. |
| Risks found | Structurally valid evidence could be replayed against another artifact with the same profile if it was not bound to artifact digest and cache object key. |
| Improvements made | Admission now fails closed with `evidence_artifact_digest_mismatch` or `evidence_cache_object_key_mismatch` before promotion. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-evidence -p whoathere-admission -p whoathere-vault-dev -p whoathere-cli`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/admission-simulations 'complete=true&evidence_subject_mismatch=true'`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 187 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Evidence metadata binding only. No public registry fetches, package serving routes, package-manager execution, detonation, object-store writes, or real package payloads. |
| Open gates | Back evidence job log digests with immutable job-log storage when scanner/detonator workers exist. |
| Next owner/action | Add scanner/detonator job schema or production admission API contracts. |

## Slice: Package-Identity Policy In Protect

| Field | Value |
| --- | --- |
| Scope | Added package-identity discovery for npm and Python manifests/argv and made `protect` fail closed on namespace-policy and direct-source identity violations before source scans, launch planning, or runner execution. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected protect/source/policy boundaries and delegated a subagent pressure test for ordering, parser scope, and validation cases. Pass 2: added bounded identity discovery for npm `package.json`, Python requirements files, and supported `pyproject.toml` dependency arrays including PEP 517 build-system requirements. Pass 3: added a `protect` package-identity gate with audit JSONL support and ecosystem-filtered workspace discovery so npm and pip manifests do not block each other. Pass 4: added CLI/source tests, permanent inert fixtures, and npm/pip smoke runs. |
| Risks found | Workspace source scanning blocked explicit registry/index overrides but did not enforce internal namespace policy from package identities; package.json direct/git/local dependency specs could bypass source-file risk gates; cross-ecosystem workspaces could accidentally block pip because of npm manifests or vice versa. |
| Improvements made | `protect` now extracts relevant identities from workspace manifests and install argv, maps source classes to policy source kinds, denies internal namespace packages resolving as public, denies untrusted direct/git/local sources under CI-fail-closed semantics, writes `protect-package-identity-gate` audit records, and runs before source scans/launch planning. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-source -p whoathere-cli`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- protect --execute --policy whoathere/tests/fixtures/package-identity/whoathere.policy --workspace whoathere/tests/fixtures/package-identity npm -- ci`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- protect --execute --policy whoathere/tests/fixtures/package-identity/whoathere.policy --workspace whoathere/tests/fixtures/package-identity pip -- install -r requirements.txt`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 197 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Identity parsing and policy evaluation only. No package-manager installs, lifecycle scripts, Python build backends, imports, public registry fetches, detonation, package serving, object-store writes, or real malicious payloads. Fixtures are inert metadata files. |
| Open gates | Add lockfile-aware source provenance for exact resolved package identities, support broader pyproject/TOML forms with a real parser, and bind package identities to future Vault admission/promoted artifact metadata. |
| Next owner/action | Build scanner/detonator job schemas or start real OS containment/egress proof providers; package identity now has enough local gating to stop being the next critical endpoint policy gap. |

## Slice: Pip Custom Requirements Argv Binding

| Field | Value |
| --- | --- |
| Scope | Bound custom pip requirements and constraint files from `pip install -r/-c`, `--requirement`, `--constraint`, and `python -m pip` argv into package-identity and source-scan gates. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: subagent pressure test confirmed custom `-r/-c` files were skipped unless reachable from known workspace filenames and called out workspace-required and symlink escape risks. Pass 2: exposed safe recursive requirements identity/source helpers in `whoathere-source`, sharing include traversal/cycle/unreadable failure behavior. Pass 3: wired CLI argv requirements parsing into package identity and source gates, including missing-workspace denial. Pass 4: hardened file reads with canonical workspace containment and added tests/smokes for custom files, equals-form options, constraints, `python -m pip`, traversal, unreadable roots through existing failures, and symlink escapes. |
| Risks found | `pip install -r custom.txt` could avoid identity and source scans if `custom.txt` was not named `requirements.txt` or included by it; reading argv files without a workspace root could inspect arbitrary paths; lexical traversal checks did not stop symlink escapes. |
| Improvements made | Custom requirement/constraint argv files now require `--workspace`, are recursively inspected for identities and source overrides, reject path traversal/cycles/unreadable files, and canonicalize candidate paths so symlinks outside the workspace fail closed. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-source -p whoathere-cli`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- protect --execute --policy whoathere/tests/fixtures/custom-requirements/whoathere.policy --workspace whoathere/tests/fixtures/custom-requirements pip -- install -r custom-requirements.txt`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- protect --execute pip -- install -r custom-requirements.txt`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- protect --execute --workspace whoathere/tests/fixtures/custom-requirements --vault-origin http://127.0.0.1:4873 pip -- install --requirement=source-override.txt`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 203 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Requirements parsing and policy/source evaluation only. No pip install, Python build backend, import, public package fetch, package serving, detonation, object-store write, or real payload execution. Fixtures are inert metadata files. |
| Open gates | Broaden pip requirement parsing only with a real parser or clearly bounded additional forms, bind resolved identities to lock/report metadata when available, and decide whether custom requirements source scans should share one global include `seen` set with default workspace files to avoid duplicate findings. |
| Next owner/action | Move to real containment/egress proof provider implementation or scanner/detonator job schemas; custom pip argv file inspection is no longer a first-order local bypass. |

## Slice: Evidence Job Metadata Binding

| Field | Value |
| --- | --- |
| Scope | Added metadata-only scanner/detonator evidence-job plans, results, and bindings; required evidence bundles to bind every job result to job id, evidence profile, artifact digest, cache object key, and canonical log digest before admission can promote. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: subagent pressure test identified anonymous evidence results, unbound logs, profile/artifact drift, and accidental execution risks. Pass 2: added `EvidenceJobResult` job ids/log digests and `EvidenceJobBinding` validation in `whoathere-evidence`. Pass 3: added `EvidenceJobRequest`, `EvidenceJobPlan`, `EvidenceJobResultRecord`, and result-binding checks in `whoathere-vault-api`, plus local-dev `/v1/evidence-job-simulations`. Pass 4: updated CLI/admission/local-dev evidence construction, smoke-ran valid and fail-closed paths, and reran full gates. |
| Risks found | Evidence could previously assert that a job kind passed without naming the job that produced it; log metadata was not bound to evidence; a future scanner/detonator API could accidentally enable execution/network/detonation through metadata routes; non-passed worker states needed fail-closed handling before admission. |
| Improvements made | Evidence validation now rejects empty or duplicate job ids, missing/unexpected/duplicate bindings, binding mismatches, non-ready bindings, and invalid `sha256:<64 lowercase hex>` log digests. Vault API planning keeps `execution_enabled=false`, `detonation_attempted=false`, and `network_attempted=false`; result binding rejects execution, detonation, network, job/profile/artifact/cache mismatches, missing audit ids, invalid log digests, and non-passed job states. Local-dev simulations expose success plus mismatch, attempted-network/detonation, and failed-worker denial paths. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-evidence -p whoathere-vault-api -p whoathere-admission -p whoathere-vault-dev -p whoathere-cli`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/evidence-job-simulations valid=true`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/evidence-job-simulations job_mismatch=true`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/evidence-job-simulations 'network_attempted=true&detonation_attempted=true'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/evidence-job-simulations worker_failed=true`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 215 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Metadata binding only. No scanner/detonator worker execution, package-manager execution, lifecycle script execution, Python build/import execution, public registry fetch, package serving, object-store write, durable log persistence, or raw log/event capture. |
| Open gates | Implement durable append-only job-log storage, signed/audited worker identities, real static/dynamic scanner workers, isolated detonation environments, and production admission APIs that authenticate tenant/job ownership. |
| Next owner/action | Add production admission/job API contracts or start a real static scanner worker skeleton that remains disabled until containment, egress, and durable logging are implemented. |

## Slice: Static Manifest Job Producer

| Field | Value |
| --- | --- |
| Scope | Added a metadata-only static manifest job producer that runs existing npm/PyPI static detector logic over inert manifest text, emits sanitized job state/reason/log-digest metadata, and binds the result through the Vault evidence-job contract. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: implemented `whoathere-hash` and detector-side `run_static_manifest_job` without adding Vault API dependency to the detector. Pass 2: added local-dev `/v1/static-manifest-job-simulations` route that plans a `StaticManifest` evidence job, runs the detector, then binds the output through `bind_evidence_job_result`. Pass 3: verifier review found malformed/escaped manifest, lifecycle coverage, identity validation, route selector, and hash-vector gaps. Pass 4: remediated those findings and reran focused tests, route smokes, full tests, format, and Clippy. |
| Risks found | Substring scanning missed escaped npm lifecycle keys and malformed metadata; lifecycle coverage did not include `prepublish`, `preprepare`, `postprepare`, `prepack`, and `postpack`; detector-side job requests could return `Passed` with invalid profile/digest/cache metadata if used outside Vault binding; the dev route defaulted unknown selectors to a clean fixture; the new hash helper only had the `abc` vector. |
| Improvements made | npm package scanning now validates supported JSON and walks decoded key paths under top-level `scripts`; malformed npm JSON fails closed; supported PyPI `pyproject.toml` shape is conservatively validated; static job requests require nonempty job/profile, nonzero profile version, canonical `sha256:<64 lowercase hex>` artifact digest, and matching `blobs/sha256/<digest>` cache key; local-dev static manifest route accepts only explicit selectors; SHA-256 has empty and 55/56/64-byte padding-boundary vectors. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-detector -p whoathere-hash -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/static-manifest-job-simulations manifest=clean_npm`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/static-manifest-job-simulations manifest=malicious_npm`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/static-manifest-job-simulations manifest=pyproject`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/static-manifest-job-simulations manifest=unknown`; `cargo fmt --manifest-path whoathere/Cargo.toml --all --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 226 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --workspace --all-targets -- -D warnings`. |
| Safety boundaries | Static job production reads inert manifest text only. It does not execute npm/pip, lifecycle scripts, Python build backends/imports, Node scripts, network fetches, dynamic detonation, package serving, object-store writes, durable log storage, or raw log capture. The local-dev route is a simulation route and not a production scanner worker. |
| Open gates | Replace conservative handwritten JSON/TOML validation with maintained parsers before broad real-world package intake; replace in-memory sanitized job logs with durable append-only storage; add signed worker identities; connect real fetched/quarantined artifacts to static scanning; replace synthetic admission evidence with real job-output bundles as worker implementations arrive. |
| Next owner/action | Add production job/admission API contracts or durable job-log persistence for static scan summaries before introducing real static worker execution over quarantined packages. |

## Slice: Immutable Sanitized Job Log Store

| Field | Value |
| --- | --- |
| Scope | Added an in-memory append-only sanitized job-log store and bound local-dev static manifest job outputs to immutable log receipts before evidence binding. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: designed a no-raw-log `whoathere-job-log` boundary that stores sanitized summaries only and returns `logs/<job_id>/<sha256>` receipts. Pass 2: integrated static manifest outputs with summary schema, sanitized summary bytes, log digest, and local-dev route storage. Pass 3: verifier review found route-local store lifetime, summary identity parsing, metadata line-injection, and raw-log structured-state gaps. Pass 4: remediated with a process-lifetime dev store, parsed summary identity validation, control-character metadata rejection, structured `raw_log_captured=false`, focused tests, route smokes, full tests, format, and Clippy. |
| Risks found | A fresh store per request could miss same-job rewrite attempts; digest-only validation let a summary claim different job/profile/artifact metadata; control characters in metadata could forge additional summary lines; route-supplied raw-log state was not tied to detector output. |
| Improvements made | `InMemoryJobLogStore::append_sanitized` now validates tenant/request/job/profile identity, canonical artifact and log digests, cache-object key, summary schema, parsed summary fields, no execution/network/detonation markers, raw-log false markers, duplicate summary fields, malformed lines, control characters, same-job immutability, and digest reuse across jobs. The static manifest route stores sanitized logs through a process-lifetime mutex, rejects raw-log and tampered-digest simulation flags before binding evidence, and has a same-job rewrite regression test. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-job-log -p whoathere-detector -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/static-manifest-job-simulations 'manifest=clean_npm&job_id=smoke-static-log-clean'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/static-manifest-job-simulations 'manifest=malicious_npm&job_id=smoke-static-log-malicious'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/static-manifest-job-simulations 'manifest=clean_npm&job_id=smoke-static-log-raw&raw_log_captured=true'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http POST /v1/static-manifest-job-simulations 'manifest=clean_npm&job_id=smoke-static-log-tamper&tamper_log_digest=true'`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 239 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`. |
| Safety boundaries | In-memory metadata storage only. No raw logs, package-manager execution, lifecycle script execution, Python build/import execution, public registry fetches, dynamic detonation, package serving, durable object-store writes, production audit writes, or worker execution. The route remains a local-dev simulation. |
| Open gates | Replace in-memory storage with durable append-only storage, add worker identity signatures, tenant-authenticated job ownership, retention policy, production audit correlation, and replay-safe admission APIs before real scanner/detonator workers or package-serving routes exist. |
| Next owner/action | Implement production job-log/admission API contracts or start the real Linux/macOS containment and Vault-only egress proof-provider slice; this job-log slice removes the immediate evidence/log mutability gap for local-dev static jobs. |

## Slice: Provider Verification Challenge Binding

| Field | Value |
| --- | --- |
| Scope | Added launch-bound provider verification challenges and hardened high-risk launch proof acceptance against replay, mixed-session proofs, weak hashes, finite-probe overclaims, and misleading execute-mode exit codes. |
| Pass count | 5 |
| Pass artifacts/evidence | Pass 1: implemented deterministic `ProviderVerificationChallenge` and `ProviderChallengeAttempt` contracts with challenge id, subject, context hash, Vault host, validation time, and probe destinations. Pass 2: rendered challenge summaries in `launch plan` and challenge attempts in `evidence providers` text/JSON output while keeping local providers fail closed. Pass 3: subagent review found challenge-subject, replay/session, FNV, finite-probe, full-challenge, and CLI exit-code issues. Pass 4: remediated challenge-subject enforcement, same provider id/platform/rule-generation session checks, requested-probe validation, and execute-mode deny exit semantics. Pass 5: upgraded launch/source/challenge binding hashes to SHA-256, required explicit default-deny egress rule attestation, reran focused/full gates, and smoke-tested CLI diagnostics. |
| Risks found | Verified containment and egress proofs could share an arbitrary subject rather than the generated launch challenge subject; separate proof objects could be stitched together from different provider/rule sessions; FNV fingerprints were not appropriate as security-binding context hashes; mandatory finite probes could be mistaken for proof of default-deny egress; `launch plan --execute` could report success for a planned high-risk install even while execution stayed disabled. |
| Improvements made | Launch plans now create SHA-256-backed provider challenges and require proof subjects to match the generated challenge subject, proof provenance context hashes to match the launch context hash, proof provider id/platform/rule generation to match across containment and egress, proof freshness to hold, and egress proof evidence to include `egress.default_deny_except_configured_vault.attested=true`. Challenge attempts reject missing requested egress probes, Vault-denied probes, non-Vault-allowed probes, provider/session mismatches, and stale proofs. CLI provider diagnostics expose challenge attempts; execute-mode launch plans deny if high-risk install execution is still disabled. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-launch -p whoathere-cli`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- launch plan --execute --workspace whoathere/tests/fixtures/source-clean --vault-origin http://127.0.0.1:4873 --egress-enforced --containment-available npm -- ci`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 248 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`. |
| Safety boundaries | Challenge generation, proof validation, and diagnostics only. No npm/pip install execution, lifecycle scripts, Python build/import execution, public registry fetches, OS namespace/VM creation, seccomp/Landlock/cgroup changes, packet-filter changes, or verified production provider minting. Test-only verified helpers remain behind the non-default `whoathere-sandbox/test-support` feature. |
| Open gates | Superseded by later challenge-first SPI and expanded-probe slices for full-challenge provider method shape and broader probe coverage. Remaining gates: implement OS-specific containment and egress collectors behind that SPI, bind proofs to nonces or persisted challenge ids, use trusted wall-clock validation, persist/reject reused challenge ids, and attest actual default-deny rule installation rather than self-reported evidence. |
| Next owner/action | Implement the first real Linux or macOS provider collector/verifier against this challenge contract while keeping `proof_verification_enabled=false` until every required check can be independently verified. |

## Slice: Challenge-First Proof Provider SPI

| Field | Value |
| --- | --- |
| Scope | Refactored proof provider evaluation so containment and egress proof attempts receive the full `ProviderVerificationChallenge` before producing proofs. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the existing provider trait, challenge attempt validation, local provider skeletons, CLI diagnostics, and the open subagent finding that containment proof generation did not receive the challenge. Pass 2: added `ProviderChallengeProofs` plus challenge-aware provider methods and wired `evaluate_challenge` through `prove_challenge`. Pass 3: made the test-only provider derive subject/context from the challenge, made Linux/macOS/generic fail-closed providers record challenge metadata while staying denied, and added regression coverage. Pass 4: focused tests exposed stale CLI assertions that expected old context-mismatch reasons; updated diagnostics tests, reran full gates, and smoke-tested text/JSON provider output plus launch planning. |
| Risks found | Generic containment proof creation could not bind to challenge subject/context; a future real provider could mint containment against stale state while egress used challenge inputs; local diagnostics previously proved failure through accidental mismatch rather than an intentional challenge-bound fail-closed result. |
| Improvements made | `ProofProvider` now has `prove_containment_for_challenge`, `prove_egress_for_challenge`, and `prove_challenge` defaults. The test-only provider proves the challenge subject/context even when its stored defaults are stale. Linux/macOS/generic fail-closed providers bind unavailable proof attempts to challenge subject/context, add challenge id/subject/context/Vault/probe-count evidence, and still fail closed with `Missing`, unimplemented, missing-probe, and non-fresh reason codes. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox`; `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-launch -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 250 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers --json`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- launch plan --execute --workspace whoathere/tests/fixtures/source-clean --vault-origin http://127.0.0.1:4873 --egress-enforced --containment-available npm -- ci` expected-denied with exit 20. |
| Safety boundaries | Provider SPI, diagnostics, and tests only. No npm/pip install execution, lifecycle scripts, Python build/import execution, public registry fetches, OS namespace/VM creation, seccomp/Landlock/cgroup changes, packet-filter changes, durable nonce storage, trusted time source, or production provider minting. |
| Open gates | Implement real Linux/macOS challenge collectors, replace in-memory challenge replay state with durable per-launch/provider persistence, use trusted wall-clock validation, independently verify actual default-deny egress rule installation across the expanded public/private/IPv6/DNS probe matrix, and decide where challenge attempts are durably audited. |
| Next owner/action | Start the real Linux provider collector/verifier slice against the challenge-first SPI, keeping provider readiness false until every required containment and egress assertion is independently verifiable. |

## Slice: Provider Challenge Replay Guard

| Field | Value |
| --- | --- |
| Scope | Added nonce, expiry, and single-use replay-control primitives for provider verification challenges. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the challenge contract, launch rendering, provider diagnostics, and remaining replay/nonce open gates from prior review. Pass 2: added nonce/expiry fields to `ProviderVerificationChallenge`, bound them into the challenge id, and added `valid_at`/`reason_codes_at` freshness checks. Pass 3: implemented `ProviderChallengeReplayGuard` with in-memory issuance, first-use acceptance, replay/unknown/expired/mutated rejection, and expiry pruning; added sandbox regression tests. Pass 4: exposed expiry/nonce-presence in launch/provider JSON diagnostics, fixed a clippy finding, reran focused/full gates, and smoke-tested CLI output. |
| Risks found | Challenge ids were deterministic from launch context and validation time only; there was no API to issue unique challenges, reject unknown challenges, reject challenge mutation, reject expiry, or prevent replay after a successful proof attempt. |
| Improvements made | Challenges now carry a nonce and expiry, and challenge ids bind nonce/expiry alongside subject, context hash, Vault host, validation time, and probes. The replay guard issues unique challenges even for identical launch inputs, consumes the first valid use, rejects subsequent use with `provider_challenge_replayed`, rejects unknown challenge ids, rejects mutation of subject/context/Vault/probes/nonce/timestamps, and prunes expired records. CLI diagnostics render expiry and nonce presence without printing the raw nonce in ordinary text output. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-launch -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 252 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers --json`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- launch plan --execute --workspace whoathere/tests/fixtures/source-clean --vault-origin http://127.0.0.1:4873 --egress-enforced --containment-available npm -- ci` expected-denied with exit 20. |
| Safety boundaries | Replay metadata only. No durable challenge store, trusted wall-clock source, npm/pip install execution, lifecycle script execution, Python build/import execution, public registry fetches, OS namespace/VM creation, seccomp/Landlock/cgroup changes, packet-filter changes, or production provider proof minting. |
| Open gates | Replace in-memory replay state with durable per-launch/provider persistence, source time from a trusted clock, wire challenge issue/consume into real provider collectors and launch proof acceptance, decide retention/audit policy for consumed challenges, and include distributed replay controls for future multi-process or Vault-backed execution. |
| Next owner/action | Implement the first real Linux provider collector or wire the replay guard into a durable local challenge service before enabling any verified high-risk install gate. |

## Slice: Expanded Vault-Only Egress Probe Matrix

| Field | Value |
| --- | --- |
| Scope | Broadened mandatory Vault-only egress proof probes beyond public package hosts to include private IPv4, IPv6, link-local/metadata, loopback, and public DNS egress classes. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected mandatory probe construction, challenge generation, verified egress tests, test-only provider helpers, and CLI probe-count assertions. Pass 2: added `vault_only_probe_destinations` as the single configured-Vault-plus-mandatory probe constructor and expanded mandatory probes. Pass 3: replaced duplicated hardcoded verified-egress destination arrays with the shared helper and added regression coverage for private IPv4, IPv6, DNS, and deduplication. Pass 4: reran focused/full gates and smoke-tested provider JSON plus launch planning to confirm 15-probe fail-closed challenges. |
| Risks found | Mandatory probes covered public registries, GitHub, metadata, and one loopback destination but omitted RFC1918/private IPv4, IPv6 loopback/ULA/link-local, and public DNS egress paths; duplicated test-support lists could drift from challenge construction. |
| Improvements made | The mandatory matrix now includes `10.0.0.1`, `172.16.0.1`, `192.168.0.1`, `[::1]:9000`, `fd00::1`, `fe80::1`, `1.1.1.1:53`, and `8.8.8.8:53` in addition to the existing registry/GitHub/metadata/loopback probes. Verified egress proof helpers consume the same matrix as challenge creation, and tests assert these added classes are denied before a proof can verify. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-sandbox -p whoathere-launch -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 253 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- evidence providers --json`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- launch plan --execute --workspace whoathere/tests/fixtures/source-clean --vault-origin http://127.0.0.1:4873 --egress-enforced --containment-available npm -- ci` expected-denied with exit 20 and `proof_challenge_probe_count=15`. |
| Safety boundaries | Probe contract and verification tests only. No live network probes, DNS queries, packet-filter changes, OS sandbox changes, npm/pip install execution, lifecycle script execution, Python build/import execution, public registry fetches, or production provider proof minting. |
| Open gates | Real OS egress providers must install and independently attest default-deny rules covering the expanded matrix, including IPv6 and DNS behavior; future policy should decide whether customer-private Vault CIDRs or internal mirrors need explicit allow-list exceptions. |
| Next owner/action | Implement a real Linux egress collector/verifier that can prove default-deny except configured Vault against this expanded matrix without allowing package-manager execution until verified. |

## Slice: Optional Python Shim Materialization

| Field | Value |
| --- | --- |
| Scope | Added explicit opt-in materialization for `python` and `python3` shims so `python -m pip` can be intercepted by teams that knowingly accept Python shim tradeoffs. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected command classification, shim manifest rendering, materialization defaults, and existing `python -m pip` package-identity/source gates. Pass 2: added `include_python` parsing and a `shims_for_materialization`/`materialize_unix_shims_with_options` path while keeping default materialization unchanged. Pass 3: added tests for opt-in parsing, dry-run flag rendering, default absence of python/python3 files, and explicit python/python3 shim creation. Pass 4: ran focused/full gates and smoke-tested dry-run plus opt-in materialization into a temp shim directory. |
| Risks found | The dry-run manifest listed python/python3 as optional, but there was no explicit materialization flag; making them default would break ordinary Python tooling because the simple shim intercepts all `python`/`python3` invocations, not only `python -m pip`. |
| Improvements made | `shim install --include-python --dest <sandbox-dir>` now installs six shims (`npm`, `npx`, `pip`, `pip3`, `python`, `python3`), while `shim install --dest <sandbox-dir>` still installs only the four default package-manager shims. Dry-run output reports `include_python=true/false` and keeps optional labels visible. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 255 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- shim install --dry-run --include-python`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- shim install --dest /private/tmp/whoathere-python-shim-smoke-codex --include-python`; `ls -la /private/tmp/whoathere-python-shim-smoke-codex` confirmed all six shim files. |
| Safety boundaries | Shim file generation only. No PATH mutation, no npm/pip install execution, no Python execution, no lifecycle scripts, no build/import execution, no registry fetches, and no automatic python/python3 interception. |
| Open gates | Design a pass-through Python shim that can safely locate the real interpreter for non-`-m pip` invocations, or keep python/python3 shims as an explicit advanced mode; add Windows shim equivalents later. |
| Next owner/action | Use the opt-in Python shim path in sandboxed integration tests for `python -m pip` interception once install execution remains guarded by verified containment and egress providers. |

## Slice: Local-Dev Registry Facade Simulation

| Field | Value |
| --- | --- |
| Scope | Added promoted-only npm/PyPI registry metadata simulation routes to the local-dev HTTP router. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected registry rendering helpers, admission generation metadata, the local-dev HTTP router, and package-serving safety gates. Pass 2: added `/v1/registry-simulations/npm/...` and `/v1/registry-simulations/pypi/...` routes backed by local inert promoted-generation metadata while keeping package-byte serving absent. Pass 3: made unpromoted route requests fail closed with HTTP 503 and `artifact_not_promoted`, and required promoted responses to use local URLs instead of public registry/index URLs. Pass 4: added vault-dev regression tests, ran focused/full gates, and smoke-tested unpromoted npm denial plus promoted npm/PyPI metadata rendering. |
| Risks found | Registry metadata helpers existed but had no local HTTP route for compatibility testing; future routes could accidentally serve unpromoted artifacts, leak public registry URLs into package-manager metadata, or enable package-byte serving before tenant authz and exact promoted-cache lookup are wired. |
| Improvements made | The local-dev router now exposes metadata-only registry simulations: unpromoted artifacts deny with 503, promoted npm renders a packument with local tarball metadata, promoted PyPI renders Simple HTML with a local href, and neither path fetches public registries or serves bytes. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-registry -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 258 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET /v1/registry-simulations/npm/fixture`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET '/v1/registry-simulations/npm/fixture?promoted=true'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET '/v1/registry-simulations/pypi/fixture?promoted=true'`. |
| Safety boundaries | Local-dev metadata rendering only. No package bytes, public registry fetches, package-manager execution, lifecycle script execution, Python build/import execution, dynamic detonation, production server exposure, object-store writes, or real package payloads. |
| Open gates | Replace the fixed local-dev generation with persistent tenant-scoped admission/cache lookup, add authz, implement content-addressed byte serving only through exact artifact-plus-key cache lookup, and complete npm/PyPI compatibility behavior before production use. |
| Next owner/action | Implement the production registry serving path backed by promoted cache generations, tenant authz, and exact artifact/key byte lookup while preserving fail-closed behavior for missing or unpromoted artifacts. |

## Slice: Local-Dev Promoted Byte Serving Simulation

| Field | Value |
| --- | --- |
| Scope | Added local-dev npm tarball and PyPI file routes that serve only fixed inert promoted cache bytes through exact artifact-plus-cache-key lookup. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the metadata routes, cache promotion API, exact promoted-byte lookup contract, and existing local-dev generation metadata. Pass 2: added npm tarball and PyPI file simulation routes before the broader metadata route matches, plus a PyPI Simple file-base renderer that avoids public index URLs. Pass 3: routed byte serving through `InMemoryCacheStore::quarantine`, `promote`, and `promoted_bytes_for_artifact`, added percent-decoding for path components, and added a deliberate cache-key mismatch denial path. Pass 4: added focused regression tests, corrected the fixed local-dev byte length to 25, ran focused/full gates, and smoke-tested byte serving plus mismatch denial through the CLI. |
| Risks found | Metadata routes pointed at local tarball/file URLs but there was no byte path; a future route could accidentally read by object key alone, serve unpromoted paths, leak public PyPI links, or let local-dev generation byte metadata drift from the actual verified payload. |
| Improvements made | npm and PyPI local-dev file routes now serve only the fixed inert payload whose SHA-256 matches the promoted artifact digest, and only via exact artifact/key cache lookup. Wrong npm/PyPI paths fail closed with `artifact_not_promoted`, cache-key mismatch fails closed with `cache_promoted_object_key_mismatch`, and promoted PyPI metadata links to the local-dev file route. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-registry -p whoathere-cache -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 264 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET /v1/registry-simulations/npm/tarballs/fixture/1.0.0`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET '/v1/registry-simulations/npm/tarballs/fixture/1.0.0?cache_key_mismatch=true'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET /v1/registry-simulations/pypi/files/pypi/fixture/1.0.0`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET '/v1/registry-simulations/pypi/fixture?promoted=true'`. |
| Safety boundaries | Local-dev inert byte serving only. No real package archives, public registry/index fetches, package-manager execution, lifecycle script execution, Python build/import execution, dynamic detonation, production server exposure, object-store writes, or real malicious payloads. The string-backed response limitation from this slice was superseded by the later binary-safe response slice. |
| Open gates | Replace fixed inert bytes with durable tenant-authenticated promoted object storage, add exact tenant authz, immutable generation lookup, and compatibility tests against npm/pip clients before exposing any production registry endpoint. |
| Next owner/action | Build the production registry data-plane contract around persistent promoted cache objects and tenant authz, while keeping install execution disabled until verified containment and Vault-only egress providers exist. |

## Slice: Bounded Local-Dev Vault Server

| Field | Value |
| --- | --- |
| Scope | Added a bounded loopback-only `vault dev-serve` command that serves the existing local-dev Vault router over real HTTP without creating an unbounded background process. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the one-shot `vault dev-http` path, loopback bind validation, and route safety constraints. Pass 2: added `serve_loopback_http`/`serve_loopback_listener` with loopback validation, nonblocking accept, max-request limit, idle timeout, and reason-coded fail-closed errors. Pass 3: wired `vault dev-serve` into CLI parsing/rendering with default loopback bind, default one-request cap, and default idle timeout; adjusted tests to avoid requiring socket permission in the default sandbox. Pass 4: ran focused/full gates and an escalated real-loopback smoke with curl. |
| Risks found | A useful local Vault needed real HTTP behavior, but a naive server command could block indefinitely, bind non-loopback interfaces, leave a process running, or be untestable under sandboxed permissions. |
| Improvements made | The dev server accepts only loopback binds, exits after `--max-requests` or `--idle-timeout-ms`, reports served request count, and fails closed for zero max requests, invalid/non-loopback binds, bind failures, and IO errors. The real smoke fetched inert promoted bytes and the server exited after exactly one request. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 270 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; escalated `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-serve --bind 127.0.0.1:48731 --max-requests 1 --idle-timeout-ms 10000`; escalated `curl -fsS http://127.0.0.1:48731/v1/registry-simulations/npm/tarballs/fixture/1.0.0`; server summary reported `served_requests=1`. |
| Safety boundaries | Local-dev loopback server only. No non-loopback bind, no long-running default, no package-manager execution, no lifecycle script execution, no Python build/import execution, no public registry fetches, no real package archives, no production auth/TLS claim, and no malware. Binding/curl smokes require explicit escalation in the current sandbox. |
| Open gates | Add production auth/TLS, durable storage, supervision, graceful shutdown signals, structured request logs, tenant authz, and npm/pip client compatibility tests before using a server mode outside local development. |
| Next owner/action | Use `vault dev-serve` as the local HTTP harness for future registry compatibility tests while keeping install execution disabled until verified containment and egress providers exist. |

## Slice: Binary-Safe Local-Dev HTTP Responses

| Field | Value |
| --- | --- |
| Scope | Converted local-dev Vault HTTP responses from string-backed bodies to byte-backed bodies and made the bounded server write exact wire bytes. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the local-dev response type, `to_http_wire`, server write path, and tests that asserted text bodies. Pass 2: added `HttpBody` with `from_text`, `from_bytes`, `as_bytes`, `contains`, and display helpers so text assertions remain readable while byte routes stay exact. Pass 3: added `to_http_wire_bytes` and switched `serve_loopback_listener` to write bytes rather than a lossy string. Pass 4: added a non-UTF-8 wire regression, reran focused/full gates, and repeated the escalated server/curl smoke after the refactor. |
| Risks found | The local-dev server could serve only UTF-8-safe payloads because `HttpResponse.body` was a `String` and the server wrote `to_http_wire(...).as_bytes()`. That would break real tarballs/wheels even if the routing and cache lookup rules were correct. |
| Improvements made | Response bodies are now byte buffers, text helpers remain ergonomic for JSON/plain/HTML tests, wire rendering has an exact byte path, and the server uses that byte path. A regression test verifies non-UTF-8 bytes survive with the correct `Content-Length`. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 271 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; escalated `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-serve --bind 127.0.0.1:48731 --max-requests 1 --idle-timeout-ms 10000`; escalated `curl -fsS http://127.0.0.1:48731/v1/registry-simulations/npm/tarballs/fixture/1.0.0`; server summary reported `served_requests=1`. |
| Safety boundaries | Response encoding and local-dev server write path only. No real package archives, public registry/index fetches, package-manager execution, lifecycle script execution, Python build/import execution, dynamic detonation, production server exposure, object-store writes, or malware. |
| Open gates | Back byte responses with durable tenant-authenticated object storage, add auth/TLS, structured request logging, immutable generation lookup, and package-manager compatibility fixtures before production use. |
| Next owner/action | Use the binary-safe local-dev server to add real inert npm tarball/wheel compatibility fixtures, while keeping installs gated until verified containment and egress providers are available. |

## Slice: Package-Byte HTTP HEAD And Cache Headers

| Field | Value |
| --- | --- |
| Scope | Added package-manager-friendly HTTP metadata for promoted local-dev byte routes without changing the promoted artifact/cache-key serving gate. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the response model, wire renderer, byte-serving routes, and package-serving safety constraints. Pass 2: added explicit response headers and a `HttpBody::head_only` representation that can declare a content length without writing body bytes. Pass 3: added cache/security headers for promoted npm/PyPI byte responses and `HEAD` routes that reuse the same promoted lookup as `GET`. Pass 4: added regression tests for GET headers, npm/PyPI HEAD length-without-body behavior, full workspace validation, and CLI/server HEAD smokes. |
| Risks found | The byte routes were exact and binary-safe, but they lacked cache validators and HEAD behavior that package managers, proxies, and CI caches commonly rely on. Adding `Accept-Ranges` without implementing range parsing would have been misleading, so this slice intentionally left range support gated until the later range slice. |
| Improvements made | Promoted byte GET responses now include `Cache-Control: private, max-age=31536000, immutable`, a SHA-256 ETag derived from the artifact digest, and `X-Content-Type-Options: nosniff`. HEAD responses for npm tarball and PyPI file routes return the same status, content type, content length, and headers with no body bytes. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 273 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http HEAD /v1/registry-simulations/npm/tarballs/fixture/1.0.0`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http HEAD /v1/registry-simulations/pypi/files/pypi/fixture/1.0.0`; escalated `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-serve --bind 127.0.0.1:48732 --max-requests 1 --idle-timeout-ms 10000`; escalated `curl -fsSI http://127.0.0.1:48732/v1/registry-simulations/npm/tarballs/fixture/1.0.0`; server summary reported `served_requests=1`. |
| Safety boundaries | Local-dev inert byte metadata only. No real package archives, byte-range serving claims, public registry/index fetches, package-manager execution, lifecycle script execution, Python build/import execution, dynamic detonation, production server exposure, object-store writes, or malware. |
| Open gates | Add conditional request behavior if needed, durable tenant-authenticated object storage, auth/TLS, structured request logging, immutable generation lookup, and real npm/pip compatibility fixtures before production use. |
| Next owner/action | Implement request/audit logging next, using the same rule: only operate after exact promoted artifact/cache-key lookup succeeds. |

## Slice: Package-Byte Single Range Serving

| Field | Value |
| --- | --- |
| Scope | Added validated single byte-range support for promoted local-dev package byte routes while preserving exact promoted artifact/cache-key lookup as the serving precondition. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected HEAD/cache-header behavior, wire rendering, range compatibility expectations, and fail-closed package-serving constraints. Pass 2: added conservative `Range` header parsing for single `bytes=start-end`, `bytes=start-`, and `bytes=-suffix` requests after promoted cache lookup. Pass 3: added `206 Partial Content`, `Content-Range`, and `Accept-Ranges: bytes` on valid promoted responses, plus `416 Range Not Satisfiable` fail-closed JSON for unsupported units, malformed ranges, multiple ranges, empty-object ranges, and unsatisfiable ranges. Pass 4: added GET/HEAD/range regression tests, full validation, and real loopback `curl` smokes for both 206 and 416 behavior. |
| Risks found | Advertising range support before implementing it would mislead package managers/proxies. Parsing ranges before promotion could also accidentally create an alternate route around the artifact/key gate. The implementation therefore performs promotion lookup first and only then slices bytes. |
| Improvements made | Promoted byte responses now advertise `Accept-Ranges: bytes`. Valid single ranges serve exact partial bytes with `206` and `Content-Range`; HEAD plus Range returns the partial metadata and declared length without body bytes. Invalid or unsupported ranges fail closed with `416`, `Content-Range: bytes */25`, and `served=false`. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 278 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; escalated `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-serve --bind 127.0.0.1:48733 --max-requests 1 --idle-timeout-ms 10000`; escalated `curl -fsS -i -H 'Range: bytes=6-10' http://127.0.0.1:48733/v1/registry-simulations/npm/tarballs/fixture/1.0.0`; escalated `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-serve --bind 127.0.0.1:48734 --max-requests 1 --idle-timeout-ms 10000`; escalated `curl -sS -i -H 'Range: bytes=99-100' http://127.0.0.1:48734/v1/registry-simulations/npm/tarballs/fixture/1.0.0`; both servers reported `served_requests=1`. |
| Safety boundaries | Local-dev inert byte serving only. No real package archives, public registry/index fetches, package-manager execution, lifecycle script execution, Python build/import execution, dynamic detonation, production server exposure, object-store writes, or malware. Range handling occurs only after exact promoted cache lookup. |
| Open gates | Add conditional request behavior if needed, durable tenant-authenticated object storage, auth/TLS, structured request logging, immutable generation lookup, and real npm/pip compatibility fixtures before production use. |
| Next owner/action | Add structured local-dev request logging for registry byte routes or move to durable authenticated object-serving contracts, while keeping install execution gated. |

## Slice: Sanitized Local-Dev Server Request Logs

| Field | Value |
| --- | --- |
| Scope | Added sanitized per-request summaries to the bounded local-dev Vault server so compatibility/debug smokes have observability without payload or secret capture. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the bounded server summary, CLI renderer, byte-serving routes, and logging privacy constraints. Pass 2: added `SanitizedRequestLogEntry` to record method, route kind, status, range state, declared response length, and wire response body byte count. Pass 3: wired summaries into `serve_loopback_listener` and `vault dev-serve` output while explicitly marking request and response bodies as not logged. Pass 4: added unit tests for server summaries, range log states, renderer output, and a real loopback smoke with an Authorization header. |
| Risks found | The local server had no per-request observability after compatibility smokes, but naive logging could leak request bodies, Authorization headers, registry tokens, full package URLs, or package bytes. |
| Improvements made | `vault dev-serve` now reports `request_log_count` and structured `request_log[N]` fields. Logs classify routes such as `registry_npm_tarball`, record `range_state` as `absent`, `served`, `rejected`, or `ignored`, and include byte counts without logging payloads. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 279 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; escalated `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-serve --bind 127.0.0.1:48735 --max-requests 1 --idle-timeout-ms 10000`; escalated `curl -fsS -i -H 'Range: bytes=6-10' ...` with a synthetic sensitive header against `http://127.0.0.1:48735/v1/registry-simulations/npm/tarballs/fixture/1.0.0`; server summary reported one sanitized log with `status_code=206`, `range_state=served`, `request_body_logged=false`, and `response_body_logged=false`; ASCII scan passed. |
| Safety boundaries | Local-dev server summaries only. No request body logging, response body logging, Authorization header logging, package byte logging, full target URL logging, package-manager execution, public registry fetches, production server exposure, or malware. |
| Open gates | Add durable production request/audit logging with tenant authorization, retention policy, signing/tamper evidence, redaction policy, and operator/admin access controls. |
| Next owner/action | Use the sanitized request logs as evidence for upcoming npm/pip compatibility smokes, then design durable production audit/request logging. |

## Slice: Socketless Dev-HTTP Synthetic Headers

| Field | Value |
| --- | --- |
| Scope | Added validated synthetic request headers to `vault dev-http` so local-dev compatibility paths such as `Range` can be tested without binding a loopback server. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected `vault dev-http` parsing/rendering, current Range tests, and header injection risks. Pass 2: added repeated `--header <name:value>` / `--header=<name:value>` parsing while preserving the existing optional body behavior. Pass 3: validated headers before routing and rejected malformed headers, CR/LF injection, empty names/values, invalid names, and simulator-owned `Host`/`Content-Length`. Pass 4: added parser/render regression tests and CLI smokes for Range success plus reserved-header fail-closed denial. |
| Risks found | Allowing arbitrary header text in the one-shot simulator could create request-smuggling-style test artifacts or duplicate simulator-owned `Host`/`Content-Length` headers, making later compatibility results unreliable. |
| Improvements made | Socketless `vault dev-http` can now exercise the same header-dependent router behavior as `vault dev-serve`, including byte ranges. Invalid or reserved headers fail closed before the request reaches the local-dev router. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-cli -p whoathere-vault-dev`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET /v1/registry-simulations/npm/tarballs/fixture/1.0.0 --header 'Range: bytes=6-10'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET /healthz --header 'Content-Length: 999'`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 282 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; ASCII scan passed. |
| Safety boundaries | Local-dev request simulation only. No socket bind, public registry fetch, package-manager execution, lifecycle script execution, Python build/import execution, real package archives, production server exposure, or malware. Headers are validated before routing. |
| Open gates | Add richer socketless compatibility scenarios for npm and pip clients, plus production HTTP normalization and proxy/header policy in the real Vault service. |
| Next owner/action | Use socketless headers for future CI-friendly compatibility tests before introducing real package-manager client smokes. |

## Slice: Conditional ETag Handling For Promoted Bytes

| Field | Value |
| --- | --- |
| Scope | Added `If-None-Match` handling for promoted local-dev package byte routes so cache validators can avoid serving bytes when the client already has the approved object. |
| Pass count | 4 |
| Pass artifacts/evidence | Pass 1: inspected the byte-serving path, cache-header slice, range ordering, and one-shot header support. Pass 2: added ETag matching after promoted artifact/cache-key lookup and before range slicing. Pass 3: supported exact strong validators, weak validators, wildcard validators, mismatched fallthrough, and matching-validator precedence over Range. Pass 4: added unit tests plus socketless CLI smokes for 304 and mismatched 200 behavior. |
| Risks found | Conditional requests must not create a pre-promotion metadata oracle or bypass the exact promoted-cache lookup. They also need deterministic ordering with Range requests to avoid inconsistent 206/304 behavior. |
| Improvements made | Matching `If-None-Match` now returns `304 Not Modified`, `Content-Length: 0`, `Accept-Ranges: bytes`, cache-control, ETag, and no body. Mismatches keep normal byte-serving behavior. Matching validators take precedence over Range. |
| Validation | `cargo test --manifest-path whoathere/Cargo.toml -p whoathere-vault-dev -p whoathere-cli`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET /v1/registry-simulations/npm/tarballs/fixture/1.0.0 --header 'If-None-Match: "<current-etag>"'`; `cargo run --manifest-path whoathere/Cargo.toml -p whoathere-cli -- vault dev-http GET /v1/registry-simulations/npm/tarballs/fixture/1.0.0 --header 'If-None-Match: "<mismatched-etag>"'`; `cargo fmt --manifest-path whoathere/Cargo.toml --all -- --check`; `cargo test --manifest-path whoathere/Cargo.toml` with 286 unit tests plus doctests; `cargo clippy --manifest-path whoathere/Cargo.toml --all-targets -- -D warnings`; ASCII scan passed. |
| Safety boundaries | Local-dev inert byte serving only. No real package archives, public registry/index fetches, package-manager execution, lifecycle script execution, Python build/import execution, dynamic detonation, production server exposure, object-store writes, or malware. Conditional handling occurs only after exact promoted cache lookup. |
| Open gates | Decide whether production Vault needs additional conditional headers such as `If-Match` or `If-Modified-Since`; add durable tenant-authenticated object storage and production HTTP cache policy. |
| Next owner/action | Use conditional request support in future npm/pip compatibility smokes and production data-plane design. |
