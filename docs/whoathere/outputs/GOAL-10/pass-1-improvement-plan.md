# Pass 1 Improvement Plan

## Planned Changes

1. Add `Readiness tier` to every GOAL-01 through GOAL-09 completion record.
2. Use `planning_ready` as the package-level tier for goals whose outputs can drive downstream plans.
3. Add qualifiers where a goal has research-required or validation-pending subcontracts.
4. Clarify older follow-up rows whose blocking state changed after GOAL-09.
5. Verify with a search that every completion record now declares a readiness tier.

## Expected Result

The readiness vocabulary introduced by GOAL-09 becomes mechanically visible across every completion record, and reviewers can verify coverage with a single command.

