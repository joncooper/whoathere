export WHOATHERE_CODESIGN_IDENTITY='Developer ID Application: Jonathan Cooper (U7BVS8X483)'
export WHOATHERE_NOTARY_PROFILE='whoathere-notary'

# Optional. Used only by the local corpus-lab container and exact-hash MalwareBazaar acquisition
# commands. Do not commit a real key.
# export MALWAREBAZAAR_AUTH_KEY='replace-with-local-key'
