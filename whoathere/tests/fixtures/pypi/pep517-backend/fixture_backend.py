def build_wheel(wheel_directory, config_settings=None, metadata_directory=None):
    print("fixture placeholder: would exercise PEP 517 behavior in sandbox")
    raise RuntimeError("fixture must not run outside WhoaThere sandbox")

