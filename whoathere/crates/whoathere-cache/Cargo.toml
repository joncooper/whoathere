[package]
name = "whoathere-cache"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
whoathere-vault-api = { path = "../whoathere-vault-api" }

[target.'cfg(unix)'.dependencies]
libc = "0.2.186"
